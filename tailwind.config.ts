/**
 * Tailwind CSS v4 通过 @tailwindcss/vite 插件配置，主要配置在 src/styles/index.css 的
 * @theme inline 块中。此文件仅作为兼容性占位，v4 下通常不需要独立配置文件。
 *
 * 如需使用 v3 兼容模式，可在此扩展：
 * https://tailwindcss.com/docs/configuration
 */
export default {}
