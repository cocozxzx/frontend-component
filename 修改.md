### 步骤 8 — 安装 shadcn/ui 导航 & 数据录入类组件

**目标**：安装导航类和数据录入类 shadcn/ui 组件，配置中文本地化

---

**Claude Code Prompt**：

```
请帮我安装 shadcn/ui 导航和数据录入类组件，具体如下：

1. 依次运行以下安装命令：
   npx shadcn@latest add tabs
   npx shadcn@latest add dropdown-menu
   npx shadcn@latest add navigation-menu
   npx shadcn@latest add breadcrumb
   npx shadcn@latest add pagination
   npx shadcn@latest add stepper
   npx shadcn@latest add select
   npx shadcn@latest add checkbox
   npx shadcn@latest add radio-group
   npx shadcn@latest add switch
   npx shadcn@latest add slider
   npx shadcn@latest add calendar
   npx shadcn@latest add date-picker
   npx shadcn@latest add number-field
   npx shadcn@latest add input-otp
   npx shadcn@latest add combobox
   npx shadcn@latest add table
   npx shadcn@latest add data-table

2. 将以下组件加入 src/components/ui/index.ts 导出：
   breadcrumb / calendar / checkbox / combobox / data-table /
   date-picker / dropdown-menu / input-otp / navigation-menu /
   number-field / pagination / radio-group / select / slider /
   stepper / switch / table / tabs

3. 配置日历中文本地化：
   安装 date-fns：npm install date-fns
   在使用 Calendar 的地方导入中文 locale：
   import { zhCN } from 'date-fns/locale'
   创建 src/lib/date.ts，导出：
   - 封装好的 formatDate(date: Date | string, fmt = 'yyyy-MM-dd'): string（使用 date-fns format）
   - parseDate(str: string): Date
   - LOCALE = zhCN（供 Calendar 使用）
   - DATE_FORMATS 常量：
     { DATE: 'yyyy-MM-dd', DATETIME: 'yyyy-MM-dd HH:mm:ss', TIME: 'HH:mm:ss', MONTH: 'yyyy-MM' }

4. 创建 src/components/ui/spin.tsx（加载旋转组件）：
   Props:
   - size?: 'sm' | 'md' | 'lg' | 'xl'（默认 md）
   - className?: string
   - tip?: string（加载文字，显示在图标下方）
   - fullscreen?: boolean（全屏遮罩）
   - spinning?: boolean（默认 true）
   - children?: ReactNode（spinning=false 时显示子内容，true 时显示加载状态）
   
   实现：
   - 图标使用 Loader2 from lucide-react，animate-spin class
   - size 对应 icon 尺寸：sm=16 md=24 lg=32 xl=48
   - fullscreen=true：fixed inset-0 z-50 bg-background/80 backdrop-blur-sm flex-center
   - 有 children 时：相对定位包裹，spinning 时子内容 opacity-50 pointer-events-none，图标绝对居中

5. 创建 src/components/ui/empty.tsx（空状态组件）：
   Props:
   - image?: ReactNode（自定义图标，默认 Inbox from lucide-react）
   - imageSize?: number（图标大小，默认 64）
   - title?: string（默认"暂无数据"）
   - description?: string
   - action?: ReactNode（操作按钮）
   - className?: string
   
   实现：
   - flex-col items-center justify-center，padding 32px
   - 图标颜色 text-muted-foreground/40
   - title text-sm text-muted-foreground，description text-xs text-muted-foreground/60
   - action 区域 mt-4

6. 创建 src/components/ui/result.tsx（结果页组件）：
   Props:
   - status: 'success' | 'error' | 'warning' | 'info' | '403' | '404' | '500'
   - title: string
   - subTitle?: string
   - extra?: ReactNode（操作按钮区）
   - icon?: ReactNode（自定义图标，覆盖默认）
   - className?: string
   
   默认图标映射（均来自 lucide-react）：
   - success: CheckCircle2（text-success，size 72）
   - error: XCircle（text-destructive，size 72）
   - warning: AlertTriangle（text-warning，size 72）
   - info: Info（text-info，size 72）
   - 403: ShieldOff（text-muted-foreground，size 72）
   - 404: FileQuestion（text-muted-foreground，size 72）
   - 500: ServerCrash（text-muted-foreground，size 72）
   
   布局：flex-col items-center，图标上方，title H3，subTitle Muted，extra 在最下方

7. 将 spin、empty、result 加入 ui/index.ts 导出

完成后确认：
- Calendar 组件显示中文（周一到周日）
- DataTable 有基础排序和过滤功能
- Spin/Empty/Result 三个组件渲染正确
```

**验收标准**：

- 所有导航和数据录入 shadcn 组件安装成功
- Calendar 显示中文日期
- Spin/Empty/Result 组件样式正确
- `ui/index.ts` 汇总导出无遗漏

---

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
