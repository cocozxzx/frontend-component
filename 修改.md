### 步骤 25 — 业务组件预览页（29 个）

**目标**：为 29 个业务组件分别创建完整的预览页面

---

**Claude Code Prompt**：

```
请帮我创建所有业务组件的预览页面，放在 src/pages/components/business/ 目录下：

每个页面同样遵循：PageHeader + DemoSection（ComponentDemo）+ PropsTable 结构

1. DatePickerPage.tsx：
   DemoSection：基础日期选择
   DemoSection：快捷选项（今天/本周/本月）
   DemoSection：禁用特定日期（disabledDate）
   DemoSection：日期时间选择（DateTimePicker）
   PropsTable

2. TimePickerPage.tsx：
   DemoSection：基础时间选择
   DemoSection：12小时制
   DemoSection：步进配置（minuteStep=15）
   DemoSection：禁用时间
   PropsTable

3. DateRangePage.tsx：
   DemoSection：基础日期范围
   DemoSection：快捷选项（最近7天/30天）
   DemoSection：禁用范围
   PropsTable

4. UploadPage.tsx：
   DemoSection：text 模式上传
   DemoSection：picture 模式上传
   DemoSection：picture-card 模式
   DemoSection：拖拽上传区域
   DemoSection：多文件上传（maxCount=5）
   DemoSection：ProUpload（sortable + previewable）
   PropsTable

5. RichEditorPage.tsx：
   DemoSection：完整工具栏编辑器
   DemoSection：自定义工具栏（只保留部分工具）
   DemoSection：只读模式（展示 HTML 渲染）
   DemoSection：字数限制（maxLength=500）
   PropsTable

6. RatePage.tsx：
   DemoSection：基础评分（可交互）
   DemoSection：半星模式
   DemoSection：只读模式
   DemoSection：自定义图标（Heart icon）
   DemoSection：自定义颜色
   DemoSection：带提示文字（tooltips）
   PropsTable

7. ColorPickerPage.tsx：
   DemoSection：基础颜色选择
   DemoSection：带透明度
   DemoSection：预设颜色
   DemoSection：HEX/RGB/HSL 格式
   PropsTable

8. VirtualListPage.tsx：
   DemoSection：固定高度虚拟列表（生成 10000 条数据）
   DemoSection：动态高度虚拟列表
   DemoSection：无限加载（onEndReached，模拟分批加载）
   PropsTable

9. DragPage.tsx：
   DemoSection：DragList 垂直拖拽排序
   DemoSection：DragList 带把手（handle）
   DemoSection：DragGrid 网格拖拽
   DemoSection：TagsInput（可拖拽排序标签）
   PropsTable

10. TreePage.tsx：
    DemoSection：基础树形展示（展开/折叠）
    DemoSection：复选框模式（父子联动）
    DemoSection：异步懒加载子节点（模拟500ms延迟）
    DemoSection：搜索过滤（高亮匹配文字）
    DemoSection：带连接线
    DemoSection：可拖拽排序
    PropsTable

11. ListPage.tsx：
    DemoSection：基础 List（带头像、标题、描述）
    DemoSection：带操作按钮的 List
    DemoSection：虚拟滚动 List（10000 项）
    DemoSection：带分页的 List
    DemoSection：加载更多模式
    PropsTable

12. TimelinePage.tsx：
    DemoSection：基础时间线（left 模式）
    DemoSection：alternate 模式（左右交替）
    DemoSection：自定义节点颜色和图标
    DemoSection：pending 进行中状态
    DemoSection：垂直时间线
    PropsTable

13. StatisticPage.tsx：
    DemoSection：基础数值（4 个并排）
    DemoSection：countUp 动画（按钮触发重新播放）
    DemoSection：带趋势箭头
    DemoSection：带前后缀（¥ + 元）
    DemoSection：loading 状态
    PropsTable

14. DescriptionsPage.tsx：
    DemoSection：基础描述列表（3列）
    DemoSection：bordered 样式
    DemoSection：垂直布局
    DemoSection：响应式列数（resize 窗口观察变化）
    DemoSection：混合 span（某项跨多列）
    PropsTable

15. ImagePage.tsx：
    DemoSection：基础图片（懒加载演示：滚动到可见区域才加载）
    DemoSection：图片预览（点击放大/缩小/旋转/下载）
    DemoSection：加载失败占位
    DemoSection：ImageGroup（多图预览，键盘切换）
    DemoSection：不同 fit 模式（contain/cover/fill）
    PropsTable

16. CarouselPage.tsx：
    DemoSection：基础轮播（带箭头和指示点）
    DemoSection：自动播放
    DemoSection：多项显示（slidesPerView=3）
    DemoSection：垂直轮播
    DemoSection：图片轮播（真实图片 URL）
    PropsTable

17. QRCodePage.tsx：
    DemoSection：基础二维码
    DemoSection：自定义颜色
    DemoSection：带 Logo（中心图片）
    DemoSection：过期状态（expired，点击刷新）
    DemoSection：下载二维码
    DemoSection：不同容错级别
    PropsTable

18. AnchorPage.tsx：
    DemoSection：右侧固定锚点（affix=true），页面内有多个高内容区
    DemoSection：嵌套锚点（二级）
    DemoSection：内嵌模式（affix=false，放在侧边栏）
    PropsTable

19-26. 图表系列（每个页面约 100-150 行）：

    LineChartPage.tsx：
    DemoSection：单系列折线
    DemoSection：多系列折线（3条线）
    DemoSection：平滑折线（smooth=true）
    DemoSection：带数据标签

    BarChartPage.tsx：
    DemoSection：基础柱状图
    DemoSection：横向柱状图
    DemoSection：堆叠柱状图
    DemoSection：带背景柱状图

    PieChartPage.tsx：
    DemoSection：基础饼图
    DemoSection：环形图（donut=true）
    DemoSection：南丁格尔玫瑰图
    DemoSection：环形图中心文字

    AreaChartPage.tsx：
    DemoSection：基础面积图
    DemoSection：多系列面积图
    DemoSection：堆叠面积图

    RadarChartPage.tsx：
    DemoSection：基础雷达图
    DemoSection：圆形雷达图
    DemoSection：多系列雷达图对比

    GaugeChartPage.tsx：
    DemoSection：基础仪表盘
    DemoSection：自定义颜色阈值（三段色）
    DemoSection：动态数值（Slider 控制）

    HeatmapChartPage.tsx：
    DemoSection：年度日历热力图（模拟 GitHub 贡献图）
    DemoSection：自定义颜色范围

    StatCardPage.tsx：
    DemoSection：4 个 StatCard 网格（销售额/订单量/用户数/转化率）
    DemoSection：带迷你折线图
    DemoSection：带迷你柱状图
    DemoSection：loading 状态

27. MapPage.tsx：
    DemoSection：基础地图（北京中心）
    DemoSection：MarkerMap（10个标记点，点击显示信息窗口）
    DemoSection：聚合地图（clusterEnabled=true，100个点）
    DemoSection：热力图（50个随机点）
    DemoSection：暗色地图（切换 mapStyle）
    PropsTable
    注意：需要配置 VITE_AMAP_KEY 环境变量，页面顶部提示如何配置

28. ProTablePage.tsx：
    DemoSection：完整 ProTable 示例（用户管理列表）
    JSON Schema 展示（展示配置代码）
    实际渲染效果（使用模拟数据，含搜索/分页/操作列/tag列）
    DemoSection：列类型展示（text/number/date/tag/badge/image/boolean/link）
    DemoSection：行内操作（confirm 二次确认）
    PropsTable（ProTable + ProTableSchema 类型说明）

29. ProFormPage.tsx：
    DemoSection：完整 ProForm 示例（设备新增表单，含 10+ 种字段类型）
    JSON Schema 展示
    实际渲染效果（含条件显示字段）
    DemoSection：分组表单（FormGroup，3个分组）
    DemoSection：ProFormRef 方法演示（外部控制提交/重置/setValue）
    PropsTable（ProForm + ProFormSchema 类型说明）

注意：
- 所有图表使用模拟数据（在文件顶部定义常量）
- 地图页面加 key 未配置时的友好提示
- 每个页面 code 示例字符串展示最简洁的使用方式
- 分批创建，每次 5-6 个文件
```

**验收标准**：

- 29 个业务组件页面均可访问
- 图表页面在切换主题色时实时更新
- 地图页面 key 未配置时不报错，显示提示信息
- ProTable/ProForm 示例数据完整，可演示所有功能

---




## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm

