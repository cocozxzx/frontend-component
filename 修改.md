### 步骤 13 — 补充 B 类组件：Watermark & Spin 增强 & Empty 增强

**目标**：完成所有 B 类业务封装组件，形成完整的 base 层

---

**Claude Code Prompt**：

```
请帮我完成剩余的 B 类业务封装组件，具体如下：

1. 创建 src/components/base/Watermark.tsx：
   
   Props：
   - content?: string | string[]（水印文字，数组时多行显示）
   - image?: string（图片水印 URL，优先级高于 content）
   - width?: number（水印单元宽度，默认 120）
   - height?: number（水印单元高度，默认 64）
   - rotate?: number（旋转角度，默认 -22）
   - opacity?: number（透明度，默认 0.15）
   - fontSize?: number（字体大小，默认 14）
   - fontColor?: string（字体颜色，默认 'rgba(0,0,0,0.15)'）
   - zIndex?: number（默认 9）
   - gap?: [number, number]（水印间距 [x, y]，默认 [100, 100]）
   - children?: ReactNode（被水印覆盖的内容区）
   - fullscreen?: boolean（全局水印，fixed 定位铺满屏幕，默认 false）
   
   实现细节：
   - 使用 Canvas 绘制水印，转为 base64 图片作为 background-image
   - 外层 div position relative，水印层 div position absolute inset-0 pointer-events-none
   - fullscreen=true 时使用 fixed 定位
   - 使用 MutationObserver 防止水印被 DevTools 删除（检测到删除时重新插入）
   - 内容变化时重新生成 Canvas

2. 创建 src/components/base/AppBadge.tsx（对 shadcn Badge 的业务扩展）：
   
   Props（继承 Badge 所有 props）：
   - count?: number（数字徽标模式）
   - overflowCount?: number（超出显示 N+，默认 99）
   - dot?: boolean（红点模式，不显示数字）
   - status?: 'success' | 'warning' | 'error' | 'processing' | 'default'（状态点模式）
   - showZero?: boolean（count=0 时是否显示，默认 false）
   - children?: ReactNode（被徽标包裹的元素）
   - offset?: [number, number]（徽标偏移 [x, y]）
   
   实现细节：
   两种模式：
   - 有 children：相对定位包裹，徽标绝对定位在右上角
   - 无 children：直接渲染 Badge 样式的 span
   
   count 模式颜色：bg-destructive text-white
   status 模式：小圆点，颜色对应：
   - success: bg-success
   - warning: bg-warning
   - error: bg-destructive
   - processing: bg-primary + 波纹动画（animate-ping）
   - default: bg-muted-foreground

3. 创建 src/hooks/useRequest.ts（请求状态管理 hook）：
   
   interface UseRequestOptions<T, P extends any[]> {
     manual?: boolean（默认 false，自动执行）
     defaultParams?: P（自动执行时的默认参数）
     initialData?: T（初始数据）
     onSuccess?: (data: T, params: P) => void
     onError?: (error: Error, params: P) => void
     onFinally?: (params: P) => void
     refreshDeps?: any[]（依赖变化时自动重新请求）
     debounceWait?: number（防抖等待时间）
     throttleWait?: number（节流等待时间）
     retryCount?: number（失败重试次数，默认 0）
     retryInterval?: number（重试间隔 ms，默认 1000）
     cacheKey?: string（缓存 key，相同 key 共享 loading 和数据）
     loadingDelay?: number（loading 延迟显示 ms，避免闪烁，默认 0）
   }
   
   返回值：
   {
     data: T | undefined
     loading: boolean
     error: Error | undefined
     run: (...params: P) => Promise<T>
     runAsync: (...params: P) => Promise<T>
     refresh: () => Promise<T>
     cancel: () => void
     mutate: (data: T | ((old: T) => T)) => void
   }
   
   实现：基于 SWR 或纯 useCallback/useRef 实现，支持取消请求（AbortController）

4. 创建 src/lib/request.ts（axios 封装）：
   
   - 创建 axios instance，baseURL 从 import.meta.env.VITE_API_BASE_URL 读取
   - 请求拦截器：自动从 localStorage 读取 token，添加 Authorization: Bearer {token} header
   - 响应拦截器：
     - 统一处理响应格式：{ code: number, data: T, message: string }
     - code !== 200 时 throw new Error(message)
     - code === 401 时：清除 token，跳转登录页，toast 提示"登录已过期"
     - code === 403 时：toast 提示"无操作权限"
     - 网络错误：toast 提示"网络连接失败，请检查网络"
     - 500 错误：toast 提示"服务器错误，请稍后重试"
   - 导出 request 对象和快捷方法 get/post/put/delete/patch
   - 导出 RequestConfig 类型（继承 AxiosRequestConfig，添加 showError?: boolean 控制是否自动提示错误）

5. 创建 src/stores/usePermissionStore.ts：
   
   State：
   - permissions: string[]（当前用户权限列表，如 ['device:view', 'device:edit']）
   - roles: string[]（当前用户角色列表）
   
   Actions：
   - setPermissions(permissions: string[], roles: string[]): void
   - hasPermission(permission: string | string[]): boolean
     单个字符串：检查是否在 permissions 中
     数组：检查是否包含其中任意一个（OR 逻辑）
   - hasAllPermissions(permissions: string[]): boolean（AND 逻辑）
   - hasRole(role: string | string[]): boolean
   - reset(): void

6. 创建 src/components/base/PermissionGuard.tsx：
   Props:
   - permission: string | string[]
   - fallback?: ReactNode（无权限时显示，默认 null）
   - children: ReactNode
   
   内部使用 usePermissionStore.hasPermission 判断

7. 将所有内容加入对应 index.ts 导出

请确保：
- Watermark 的 MutationObserver 在组件卸载时 disconnect
- AppBadge processing 状态的波纹动画正常
- useRequest 的 cancel 能真正取消正在进行的请求（AbortController）
- request.ts 的错误提示不会在 manual=false（用户手动取消）时触发
```

**验收标准**：

- Watermark 在 DevTools 删除水印 DOM 后自动重新插入
- AppBadge count 超过 overflowCount 显示 `99+`
- useRequest 自动请求、手动请求、刷新均正常
- axios 拦截器 401 自动跳转登录，不会重复跳转

---


## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
