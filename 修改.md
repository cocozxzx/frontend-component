### 步骤 23 — 组件预览页框架 & 公共组件

**目标**：建立组件预览页的统一框架，包含代码展示、Props 表格、交互控制等公共组件

---

**Claude Code Prompt**：

```
请帮我搭建组件预览页的基础框架和公共组件，具体如下：

1. 创建 src/components/preview/ComponentDemo.tsx（单个组件展示块）：

   Props：
   - title: string（示例标题，如"基础用法"）
   - description?: string（示例描述）
   - code: string（示例代码字符串）
   - children: ReactNode（实际渲染的组件）
   - defaultShowCode?: boolean（默认展开代码，默认 false）
   - className?: string

   布局：
   - 上方：组件实际渲染区（白色背景，padding 24px，border，rounded）
   - 下方折叠区：代码展示
   - 右上角：展开/折叠代码按钮（Code icon）+ 复制代码按钮（Copy icon）
   - 代码展示：使用 <pre> + <code>，安装 highlight.js 做语法高亮
     npm install highlight.js
     import 'highlight.js/styles/github.css'（亮色）或 'github-dark.css'（暗色）
     根据 useAppStore.colorMode 切换样式

2. 创建 src/components/preview/PropsTable.tsx（Props 说明表格）：

   PropItem 类型：
   {
     name: string         // prop 名称
     type: string         // 类型（字符串，如 'string | number'）
     default?: string     // 默认值
     required?: boolean   // 是否必填
     description: string  // 说明
   }

   Props：
   - data: PropItem[]
   - title?: string（默认"Props"）
   - className?: string

   实现：
   - shadcn Table 渲染
   - name 列：code 标签样式（bg-muted font-mono text-sm rounded px-1）
   - type 列：蓝色 code 标签（text-primary）
   - required 列：必填显示红色 * 或 Badge

3. 创建 src/components/preview/PageHeader.tsx（组件预览页头部）：

   Props：
   - title: string（组件名称，如"Button 按钮"）
   - description: string（组件描述）
   - tags?: string[]（标签，如 ['shadcn/ui', '基础组件']）
   - sourceUrl?: string（源码链接）

   布局：
   - 大标题（H1 样式）+ 描述文字
   - 下方 tag 列表（Tag 组件）
   - 右侧可选源码链接（ExternalLink icon）
   - 底部分割线

4. 创建 src/components/preview/DemoSection.tsx（章节包裹器）：

   Props：
   - title: string（章节标题，如"基础用法"、"尺寸"、"状态"）
   - children: ReactNode

   实现：
   - 标题 H2 样式（text-lg font-semibold，左侧 3px 主色竖线）
   - children 下方展示
   - 多个 ComponentDemo 之间有间距

5. 创建 src/components/preview/index.ts 导出所有预览工具组件

6. 创建 src/pages/components/index.tsx（组件库首页）：

   展示所有组件的入口卡片网格：
   
   两个分组：
   
   【基础组件】（30 个，对应路由 /components/basic/*）
   卡片信息：组件名 + 英文名 + 简介 + 点击跳转
   
   组件列表及简介：
   - Button 按钮：触发操作的基础按钮
   - Input 输入框：单行文本输入
   - Select 选择器：下拉选择数据
   - Form 表单：数据录入与校验容器
   - Table 表格：展示结构化数据
   - Modal 弹窗：对话框交互
   - Drawer 抽屉：侧滑抽屉面板
   - Tabs 标签页：内容切换
   - Card 卡片：内容容器
   - Badge 徽标：数字或状态标记
   - Avatar 头像：用户头像展示
   - Tooltip 文字提示：悬浮文字提示
   - Popover 气泡卡片：悬浮卡片
   - Alert 警告：页内警告信息
   - Toast 消息：全局提示消息
   - Progress 进度条：任务进度
   - Skeleton 骨架屏：内容加载占位
   - Empty 空状态：无数据展示
   - Result 结果页：操作结果反馈
   - Spin 加载中：加载状态
   - Pagination 分页：数据分页
   - Breadcrumb 面包屑：页面层级导航
   - Steps 步骤条：引导操作步骤
   - Collapse 折叠面板：内容折叠展开
   - Divider 分割线：内容分隔
   - Tag 标签：标记和分类
   - Typography 排版：文字样式规范
   - Icon 图标：图标库展示
   - ScrollArea 滚动区域：自定义滚动条
   - Watermark 水印：页面水印
   
   【业务组件】（29 个，对应路由 /components/business/*）
   - DatePicker 日期选择：日期录入
   - TimePicker 时间选择：时间录入
   - DateRange 日期范围：范围日期选择
   - Upload 上传：文件上传
   - RichEditor 富文本：内容编辑器
   - Rate 评分：星级评分
   - ColorPicker 颜色：颜色选择器
   - VirtualList 虚拟列表：大数据量列表
   - DragDrop 拖拽：拖拽排序
   - Tree 树形：层级数据展示
   - List 列表：数据列表
   - Timeline 时间线：时序内容
   - Statistic 统计：数值展示
   - Descriptions 描述：键值对展示
   - Image 图片：图片展示与预览
   - Carousel 轮播：内容轮播
   - QRCode 二维码：二维码生成
   - Anchor 锚点：页内导航
   - LineChart 折线图：趋势数据
   - BarChart 柱状图：对比数据
   - PieChart 饼图：占比数据
   - AreaChart 面积图：趋势面积
   - RadarChart 雷达图：多维对比
   - GaugeChart 仪表盘：进度指标
   - HeatmapChart 热力图：日历热力
   - StatCard 数值卡片：关键指标
   - Map 地图：地理信息展示
   - ProTable 高级表格：Schema 驱动列表
   - ProForm 高级表单：Schema 驱动表单

   卡片样式：
   - hover 时 border-primary shadow-md translate-y-[-2px]（CSS transition）
   - 左上角彩色小方块（各组件随机主题色，但固定不变）
   - 底部显示"查看详情 →"

请确保：
- ComponentDemo 代码高亮在亮/暗色模式自动切换样式
- 首页网格在移动端自适应（1列 → 2列 → 3列 → 4列）
- 点击卡片 Link 跳转对应路由
```

---



## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm

