### 步骤 14 — 表单增强组件（Rate / ColorPicker / TimePicker / RichEditor）

**目标**：封装 shadcn/ui 未提供的表单录入类组件，全部支持 React Hook Form 集成

---

**Claude Code Prompt**：

```
请帮我封装四个表单增强组件，具体如下：

1. 创建 src/components/form/Rate.tsx（评分组件）：

   Props：
   - value?: number（当前评分，0 到 count 之间）
   - onChange?: (value: number) => void
   - count?: number（星星总数，默认 5）
   - allowHalf?: boolean（允许半星，默认 false）
   - allowClear?: boolean（再次点击当前值则清零，默认 true）
   - disabled?: boolean
   - readonly?: boolean（只读，不可交互）
   - size?: 'sm' | 'md' | 'lg'（默认 'md'，对应 16/20/24px）
   - icon?: ReactNode（自定义图标，默认 Star from lucide-react）
   - activeIcon?: ReactNode（激活状态图标，默认与 icon 相同但填充色）
   - activeColor?: string（激活颜色，默认使用 --warning CSS 变量）
   - inactiveColor?: string（未激活颜色，默认 text-muted-foreground）
   - tooltips?: string[]（每个星星的 tooltip 文字，长度需等于 count）
   - className?: string

   实现细节：
   - 使用 useState 管理 hoverValue（鼠标悬停预览）
   - 渲染时：value = hoverValue ?? actualValue
   - allowHalf：鼠标在星星左半边时为半星（mouseMove 判断 offsetX）
   - 每个星星 wrapper 使用 relative，半星用 clip-path 或两层叠加实现
   - disabled/readonly 时 pointer-events-none
   - tooltips 有值时用 Tooltip 包裹每个星星

2. 创建 src/components/form/ColorPicker.tsx（颜色选择器）：

   Props：
   - value?: string（颜色值，支持 HEX/RGB/HSL 格式）
   - onChange?: (color: string, colorObj: ColorObject) => void
   - format?: 'hex' | 'rgb' | 'hsl'（输出格式，默认 'hex'）
   - presets?: string[]（预设颜色数组）
   - showPresets?: boolean（显示预设色板，默认 true）
   - showAlpha?: boolean（显示透明度滑块，默认 false）
   - disabled?: boolean
   - trigger?: ReactNode（自定义触发器）
   - className?: string

   ColorObject 类型：
   { hex: string; rgb: { r: number; g: number; b: number; a: number }; hsl: { h: number; s: number; l: number; a: number } }

   实现细节（基于 chroma-js）：
   - Trigger：一个正方形色块（12x12 或 16x16）+ 当前颜色值文字，点击打开 Popover
   - Popover 内部布局（从上到下）：
     a. 颜色面板（Saturation/Brightness 二维选择区）：
        使用 Canvas 或 CSS 渐变（background: linear-gradient 叠加实现 HSB 色域）
        可拖拽小圆形指示器
     b. 色相滑块（Hue slider，rainbow 渐变）
     c. 透明度滑块（showAlpha=true 时显示，棋盘格背景 + 当前色渐变）
     d. 输入框区：HEX 输入 + RGB/HSL 三个数字输入（format 切换）
     e. 预设色板（showPresets=true 时显示，每行 8 个小圆形色块）
   - 默认预设色：shadcn 颜色系统的 12 种主色

3. 创建 src/components/form/TimePicker.tsx（时间选择器）：

   Props：
   - value?: string（时间字符串，格式 'HH:mm:ss'）
   - onChange?: (time: string) => void
   - format?: string（默认 'HH:mm:ss'，可设 'HH:mm' 只显示时分）
   - disabled?: boolean
   - placeholder?: string（默认"请选择时间"）
   - hourStep?: number（小时步进，默认 1）
   - minuteStep?: number（分钟步进，默认 1）
   - secondStep?: number（秒步进，默认 1）
   - disabledTime?: () => { disabledHours?: number[]; disabledMinutes?: number[]; disabledSeconds?: number[] }
   - allowClear?: boolean（默认 true）
   - use12Hours?: boolean（12 小时制，默认 false）
   - className?: string

   实现细节：
   - Trigger：AppInput（带 Clock icon suffix，allowClear）
   - Popover 内：三列滚动选择（时 / 分 / 秒）
   - 每列使用 ScrollArea，当前选中项自动滚动居中（useEffect + scrollIntoView）
   - 选中项高亮（bg-primary text-primary-foreground）
   - disabledTime 返回的数组中的项显示为 muted，不可点击
   - use12Hours=true 时右侧额外一列 AM/PM 选择

4. 创建 src/components/form/DateTimePicker.tsx（日期时间选择器）：

   Props：
   - value?: Date | string | null
   - onChange?: (date: Date | null, dateStr: string) => void
   - placeholder?: string（默认"请选择日期时间"）
   - format?: string（默认 'yyyy-MM-dd HH:mm:ss'）
   - disabled?: boolean
   - disabledDate?: (date: Date) => boolean
   - allowClear?: boolean
   - className?: string

   实现：AppDatePicker 的 showTime=true 模式的独立封装版本
   Popover 内：左侧日历 + 右侧 TimePicker，底部"确认"按钮

5. 创建 src/components/editor/RichEditor.tsx（富文本编辑器）：

   安装依赖：
   @tiptap/react @tiptap/starter-kit @tiptap/extension-image
   @tiptap/extension-link @tiptap/extension-underline
   @tiptap/extension-text-align @tiptap/extension-color
   @tiptap/extension-highlight @tiptap/extension-placeholder
   @tiptap/extension-character-count @tiptap/extension-code-block-lowlight
   lowlight

   Props：
   - value?: string（HTML 字符串）
   - onChange?: (html: string) => void
   - placeholder?: string（默认"请输入内容..."）
   - readonly?: boolean
   - minHeight?: string（默认 '200px'）
   - maxHeight?: string（超出滚动）
   - maxLength?: number（最大字数）
   - uploadImageFn?: (file: File) => Promise<string>（图片上传，返回 URL）
   - toolbar?: ToolbarItem[]（自定义工具栏，默认全部）
   - className?: string

   ToolbarItem 可选值：
   'bold' | 'italic' | 'underline' | 'strike' | 'code' |
   'h1' | 'h2' | 'h3' | 'bulletList' | 'orderedList' |
   'blockquote' | 'codeBlock' | 'link' | 'image' |
   'textAlign' | 'color' | 'highlight' | 'clear' | '|'（分割线）

   实现细节：
   - EditorToolbar 单独组件（src/components/editor/EditorToolbar.tsx）
   - 工具栏图标全部使用 lucide-react
   - 工具栏按钮激活状态：isActive() 返回 true 时 bg-muted
   - 图片上传：点击图片按钮 → input[type=file] 选择 → uploadImageFn → 插入图片节点
   - 链接：点击链接按钮 → 弹出 Popover 输入 URL → 插入/更新链接
   - 代码块：使用 lowlight 语法高亮（支持 js/ts/css/html/json/python/bash）
   - maxLength：底部显示字数 "当前字数 / maxLength"，超出时阻止输入
   - 只读模式：隐藏工具栏，编辑器 contenteditable=false
   - 样式：工具栏与编辑区之间有 border，编辑区内有 prose 排版样式

6. 创建 src/components/form/index.ts 导出所有 form 组件：
   export { Rate } from './Rate'
   export { ColorPicker } from './ColorPicker'
   export { TimePicker } from './TimePicker'
   export { DateTimePicker } from './DateTimePicker'
   export { RichEditor, EditorToolbar } from '../editor/RichEditor'

请确保：
- Rate 半星渲染在所有浏览器一致
- ColorPicker 颜色面板拖拽流畅（mousemove 事件不卡顿）
- TimePicker 列滚动时当前项自动居中动画流畅（scroll-behavior: smooth）
- RichEditor onChange 不会在每次 keydown 都触发（使用 onUpdate 防抖 300ms）
```

**验收标准**：

- Rate 半星模式鼠标悬停预览正确
- ColorPicker 输入 HEX 值后色板联动更新
- TimePicker 禁用时间项不可点击且视觉变暗
- RichEditor 图片上传后正确插入编辑器

---



## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
