### 步骤 2 — 配置 Tailwind CSS 4 + shadcn/ui 初始化

**目标**：安装并配置 Tailwind CSS 4，初始化 shadcn/ui，建立 CSS 变量体系

**执行内容**：

1. 安装 Tailwind CSS 4 + PostCSS
2. 配置 `tailwind.config.ts`（语义化 Token）
3. 初始化 shadcn/ui（`npx shadcn@latest init`）
4. 生成 `components.json`
5. 创建 `src/styles/globals.css`（含完整 CSS 变量）
6. 创建 `src/lib/utils.ts`（cn 函数）

---

**Claude Code Prompt**：

```
请帮我配置 Tailwind CSS 4 和 shadcn/ui，具体如下：

1. 安装依赖：
   tailwindcss@latest @tailwindcss/vite postcss autoprefixer
   @tailwindcss/typography @tailwindcss/forms

2. 在 vite.config.ts 中添加 @tailwindcss/vite 插件

3. 创建 tailwind.config.ts，内容要求：
   - darkMode: ['class']
   - content 扫描 src/**/*.{ts,tsx}
   - theme.extend.colors 全部使用 CSS 变量：
     border/input/ring/background/foreground
     primary(DEFAULT + foreground)
     secondary(DEFAULT + foreground)
     destructive(DEFAULT + foreground)
     muted(DEFAULT + foreground)
     accent(DEFAULT + foreground)
     popover(DEFAULT + foreground)
     card(DEFAULT + foreground)
     success(DEFAULT + foreground)
     warning(DEFAULT + foreground)
     info(DEFAULT + foreground)
     sidebar(DEFAULT + foreground + border + accent + accent-foreground + primary + primary-foreground + ring)
   - theme.extend.borderRadius: lg/md/sm 均使用 var(--radius) 计算
   - theme.extend.fontFamily.sans 使用 var(--font-sans)
   - 配置 keyframes: accordion-down/up, fade-in, slide-in-from-top, collapsible-down/up
   - 配置对应 animation

4. 创建 src/styles/globals.css，内容要求：
   @import "tailwindcss";
   
   :root {
     /* JetLinks 风格 - 科技蓝主题 */
     --background: 0 0% 100%;
     --foreground: 210 11% 15%;
     --card: 0 0% 100%;
     --card-foreground: 210 11% 15%;
     --popover: 0 0% 100%;
     --popover-foreground: 210 11% 15%;
     --primary: 210 100% 56%;        /* #1890FF 科技蓝 */
     --primary-foreground: 0 0% 100%;
     --secondary: 210 40% 96%;
     --secondary-foreground: 210 11% 15%;
     --muted: 210 40% 96%;
     --muted-foreground: 215 16% 47%;
     --accent: 210 40% 96%;
     --accent-foreground: 210 11% 15%;
     --destructive: 0 84% 60%;       /* #FF4D4F */
     --destructive-foreground: 0 0% 100%;
     --success: 100 77% 44%;         /* #52C41A */
     --success-foreground: 0 0% 100%;
     --warning: 35 92% 51%;          /* #FAAD14 */
     --warning-foreground: 0 0% 100%;
     --info: 210 100% 56%;
     --info-foreground: 0 0% 100%;
     --border: 214 32% 91%;
     --input: 214 32% 91%;
     --ring: 210 100% 56%;
     --radius: 0.375rem;
     --font-sans: 'PingFang SC', 'Microsoft YaHei', 'Helvetica Neue', sans-serif;
     
     /* Sidebar - JetLinks 深色侧边栏 */
     --sidebar: 209 61% 16%;           /* #001529 深海军蓝 */
     --sidebar-foreground: 0 0% 85%;
     --sidebar-border: 209 61% 20%;
     --sidebar-accent: 210 100% 56%;
     --sidebar-accent-foreground: 0 0% 100%;
     --sidebar-primary: 210 100% 56%;
     --sidebar-primary-foreground: 0 0% 100%;
     --sidebar-ring: 210 100% 56%;
     
     /* 内容区背景 */
     --page-bg: 220 14% 96%;           /* #F0F2F5 */
   }
   
   .dark {
     --background: 222 84% 5%;
     --foreground: 210 40% 98%;
     --card: 222 84% 5%;
     --card-foreground: 210 40% 98%;
     --popover: 222 84% 5%;
     --popover-foreground: 210 40% 98%;
     --primary: 210 100% 56%;
     --primary-foreground: 0 0% 100%;
     --secondary: 217 33% 17%;
     --secondary-foreground: 210 40% 98%;
     --muted: 217 33% 17%;
     --muted-foreground: 215 20% 65%;
     --accent: 217 33% 17%;
     --accent-foreground: 210 40% 98%;
     --destructive: 0 63% 31%;
     --destructive-foreground: 210 40% 98%;
     --success: 100 77% 44%;
     --success-foreground: 0 0% 100%;
     --warning: 35 92% 51%;
     --warning-foreground: 0 0% 0%;
     --border: 217 33% 17%;
     --input: 217 33% 17%;
     --ring: 210 100% 56%;
     --sidebar: 222 84% 4%;
     --sidebar-foreground: 210 40% 85%;
     --sidebar-border: 217 33% 12%;
     --page-bg: 222 84% 5%;
   }
   
   * { @apply border-border; }
   body {
     @apply bg-[hsl(var(--page-bg))] text-foreground;
     font-family: var(--font-sans);
   }
   /* 滚动条美化 */
   ::-webkit-scrollbar { width: 6px; height: 6px; }
   ::-webkit-scrollbar-track { background: transparent; }
   ::-webkit-scrollbar-thumb { @apply bg-border rounded-full; }
   ::-webkit-scrollbar-thumb:hover { @apply bg-muted-foreground/40; }
   
   /* 工具类 */
   .flex-center { @apply flex items-center justify-center; }
   .ellipsis { @apply overflow-hidden whitespace-nowrap text-ellipsis; }
   .absolute-center { @apply absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2; }

5. 运行 npx shadcn@latest init，配置：
   - style: default
   - base color: blue
   - CSS variables: yes
   - tailwind config: tailwind.config.ts
   - components alias: @/components/ui
   - utils alias: @/lib/utils

6. 创建 src/lib/utils.ts：
   import { type ClassValue, clsx } from 'clsx'
   import { twMerge } from 'tailwind-merge'
   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   同时导出以下工具函数：
   - formatDate(date, format): 日期格式化（用 dayjs）
   - formatNumber(num, decimals): 数字千分位格式化
   - formatFileSize(bytes): 文件大小格式化（B/KB/MB/GB）
   - deepClone<T>(obj): 深拷贝
   - debounce(fn, delay): 防抖
   - throttle(fn, delay): 节流
   - generateId(prefix): 生成唯一 ID
   - sleep(ms): 延时 Promise

7. 在 main.tsx 中 import './styles/globals.css'

完成后确认页面样式正常加载。
```

**验收标准**：

- Tailwind 类名生效，页面有基础样式
- shadcn/ui `components.json` 已生成
- CSS 变量在浏览器 DevTools 中可见
- `cn()` 函数可正常调用

---

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
