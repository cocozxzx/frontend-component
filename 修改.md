### 步骤 20 — 高级组件（DragDrop / VirtualSelect / TagsInput）

**目标**：封装拖拽排序、虚拟下拉、标签输入三个高级交互组件

---

**Claude Code Prompt**：

```
请帮我封装 DragList、DragGrid、VirtualSelect、TagsInput 四个高级组件，具体如下：

1. 创建 src/components/advanced/DragList.tsx（可拖拽排序列表）：

   Props：
   - items: Array<{ id: string | number; [key: string]: any }>（数据，必须有唯一 id）
   - renderItem: (item: T, index: number, isDragging: boolean) => ReactNode
   - onReorder: (items: T[]) => void（排序完成回调，返回新顺序的完整数组）
   - direction?: 'vertical' | 'horizontal'（默认 'vertical'）
   - handle?: boolean（显示拖拽把手图标，默认 false，false 时整行可拖拽）
   - disabled?: boolean
   - animationDuration?: number（排序动画时长 ms，默认 200）
   - className?: string
   - itemClassName?: string

   实现细节（基于 @dnd-kit/sortable）：
   - 使用 SortableContext + useSortable
   - isDragging=true 时：item 半透明（opacity-50）+ 边框高亮
   - 占位符（Overlay）：显示被拖拽项的镜像，fixed 定位跟随鼠标
   - handle=true：左侧 GripVertical icon，仅图标区域可拖拽
   - 动画：CSS transition transform 实现平滑位移

2. 创建 src/components/advanced/DragGrid.tsx（可拖拽网格）：

   Props：
   - items: Array<{ id: string | number; [key: string]: any }>
   - renderItem: (item: T, index: number, isDragging: boolean) => ReactNode
   - onReorder: (items: T[]) => void
   - columns?: number（列数，默认 3）
   - gap?: number（间距 px，默认 16）
   - disabled?: boolean
   - className?: string

   实现（基于 @dnd-kit/sortable，strategy 使用 rectSortingStrategy）

3. 创建 src/components/advanced/VirtualSelect.tsx（万级数据虚拟滚动下拉）：

   Props：
   - options: Array<{ label: string; value: string | number; disabled?: boolean; group?: string }>
   - value?: string | number | Array<string | number>
   - onChange?: (value: string | number | Array<string | number>, options: OptionItem | OptionItem[]) => void
   - multiple?: boolean（多选模式，默认 false）
   - searchable?: boolean（本地搜索，默认 true）
   - placeholder?: string（默认"请选择"）
   - disabled?: boolean
   - loading?: boolean
   - maxTagCount?: number（多选时最多显示几个 Tag，超出显示 +N，默认 3）
   - clearable?: boolean（可清空，默认 true）
   - groupBy?: string（按 group 字段分组显示）
   - itemHeight?: number（每个选项高度 px，默认 36）
   - maxHeight?: number（下拉最大高度 px，默认 300）
   - emptyText?: string（默认"暂无数据"）
   - className?: string

   实现细节：
   - 下拉列表使用 @tanstack/react-virtual 渲染（支持 100000 个选项流畅滚动）
   - 搜索：useMemo 过滤 options，虚拟列表重置到顶部
   - 多选：Trigger 显示已选 Tag，超出 maxTagCount 显示 "+N 项"
   - 分组：groupBy 字段相同的选项归在一个分组标题下（分组标题不可选）
   - 键盘操作：↑↓ 移动焦点，Enter 选中，Backspace 删除最后一个 Tag（多选），Escape 关闭

4. 创建 src/components/advanced/TagsInput.tsx（标签输入组件）：

   Props：
   - value?: string[]（标签数组）
   - onChange?: (tags: string[]) => void
   - placeholder?: string（默认"输入后按 Enter 添加"）
   - maxCount?: number（最多标签数量）
   - maxTagLength?: number（单个标签最大字符数）
   - allowDuplicate?: boolean（允许重复，默认 false）
   - separator?: string | string[]（分隔符，默认 ['Enter', ',']，输入对应字符时自动创建标签）
   - validator?: (tag: string) => boolean | string（标签校验，返回 string 为错误信息）
   - suggestions?: string[]（输入时显示建议下拉）
   - disabled?: boolean
   - readOnly?: boolean
   - className?: string

   实现细节：
   - 显示已有 Tag（可关闭）+ 输入框（flex-wrap 布局）
   - 输入框宽度自适应（最小 120px）
   - 按 separator 触发添加：trim 后非空则添加，已有则 shake 动画提示
   - validator 返回 string：输入框红色边框 + 错误文字提示（2s 后消失）
   - suggestions：Popover 下拉，点击或 Enter 添加
   - 点击已有 Tag 的 X 删除
   - Backspace 且输入框为空时：删除最后一个 Tag

5. 创建 src/components/advanced/index.ts 导出：
   export { DragList } from './DragList'
   export { DragGrid } from './DragGrid'
   export { VirtualSelect } from './VirtualSelect'
   export { TagsInput } from './TagsInput'

请确保：
- DragList 拖拽时原位置显示占位（高度保留，半透明）
- DragGrid 跨行拖拽时动画正确（使用 rectSortingStrategy）
- VirtualSelect 100000 条选项搜索过滤后列表立即响应（< 50ms）
- TagsInput separator 为逗号时，粘贴 "a,b,c" 能批量创建三个标签
```

**验收标准**：

- DragList 拖拽排序后 onReorder 数据顺序正确
- DragGrid 从第 1 格拖到第 9 格，中间元素正确位移
- VirtualSelect 10 万条数据滚动流畅（无白屏）
- TagsInput 粘贴批量内容正确分割创建多个标签

---


## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm

