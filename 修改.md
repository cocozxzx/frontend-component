### 步骤 11 — AppTable & AppForm & AppPagination

**目标**：封装高频使用的表格、表单容器、分页组件

---

**Claude Code Prompt**：

```
请帮我封装 AppTable、AppForm、AppPagination 三个业务组件，具体如下：

1. 创建 src/components/base/AppTable.tsx：
   
   基于 shadcn Table + @tanstack/react-table 封装，Props：
   - columns: ColumnDef<TData>[]（TanStack Table 列定义）
   - data: TData[]（表格数据）
   - loading?: boolean（显示 Spin 遮罩）
   - rowKey?: string（行唯一键，默认 'id'）
   - striped?: boolean（斑马纹，默认 false）
   - bordered?: boolean（显示边框，默认 false）
   - size?: 'sm' | 'md' | 'lg'（行高，默认 'md'）
   - emptyText?: string（空数据提示，默认"暂无数据"）
   - emptyImage?: ReactNode（空数据图标）
   - rowSelection?: boolean（启用行选择）
   - onRowSelectionChange?: (rows: TData[]) => void
   - onRowClick?: (row: TData) => void
   - stickyHeader?: boolean（固定表头，默认 false）
   - maxHeight?: string | number（设置后启用 stickyHeader）
   - columnVisibility?: boolean（显示列配置按钮，允许用户控制列显隐）
   - className?: string
   
   实现细节：
   - 使用 useReactTable hook，启用 getCoreRowModel、getSortedRowModel、getFilteredRowModel
   - loading 时：表格内容区显示 Spin（相对定位遮罩）
   - 空数据时：tbody 中渲染 Empty 组件（colspan 撑满）
   - striped：奇数行 bg-muted/30
   - size 对应 TableCell padding：sm=py-1 px-3, md=py-2 px-4, lg=py-3 px-4
   - rowSelection=true：第一列自动添加 Checkbox 列
   - columnVisibility=true：右上角显示 Settings2 图标按钮，点击弹出 Popover 列显隐控制

2. 安装依赖：npm install @tanstack/react-table

3. 创建 src/components/base/AppForm.tsx：
   
   基于 react-hook-form + zod 的表单容器，Props：
   - schema: ZodSchema（Zod 校验 schema）
   - defaultValues?: Record<string, any>
   - onSubmit: (values: z.infer<typeof schema>) => void | Promise<void>
   - loading?: boolean（提交 loading）
   - children: ReactNode | ((form: UseFormReturn) => ReactNode)（支持 render props）
   - layout?: 'vertical' | 'horizontal'（默认 'vertical'）
   - labelWidth?: string（horizontal 布局时标签宽度，默认 '100px'）
   - className?: string
   
   同时创建配套的 FormField 组件（类似 shadcn FormField，但集成更简单）：
   
   src/components/base/FormField.tsx：
   Props:
   - name: string（字段名，对应 schema key）
   - label?: string
   - required?: boolean（显示红色星号）
   - tooltip?: string（label 右侧问号 Tooltip）
   - children: ReactNode | ((field: ControllerRenderProps) => ReactNode)
   
   从 FormContext 读取 control、errors，自动包裹 FormItem + FormLabel + FormControl + FormMessage

4. 创建 src/components/base/AppPagination.tsx：
   
   Props：
   - total: number（总条数）
   - page: number（当前页，从 1 开始）
   - pageSize: number（每页条数）
   - onPageChange: (page: number) => void
   - onPageSizeChange?: (pageSize: number) => void
   - pageSizeOptions?: number[]（默认 [10, 20, 50, 100]）
   - showTotal?: boolean（显示"共 N 条"，默认 true）
   - showQuickJump?: boolean（显示跳页输入框，默认 true）
   - showPageSize?: boolean（显示每页条数选择，默认 true）
   - disabled?: boolean
   - className?: string
   
   实现细节：
   - 布局：left = "共 X 条" | center = 页码按钮 | right = 每页条数 + 跳页
   - 使用 shadcn Pagination 组件渲染页码按钮
   - 总页数超过 7 时显示省略号（...）
   - 跳页输入框：输入数字后按 Enter 或点击"跳转"按钮跳页，超出范围自动 clamp
   - 每页条数使用 shadcn Select 组件

5. 创建 src/hooks/useTable.ts（表格逻辑 hook）：
   
   Props：
   interface UseTableOptions<T> {
     fetchFn: (params: { page: number; pageSize: number; [key: string]: any }) => Promise<{ list: T[]; total: number }>
     defaultParams?: Record<string, any>
     defaultPageSize?: number
     immediate?: boolean  // 默认 true，初始化时自动请求
   }
   
   返回值：
   {
     data: T[]
     total: number
     loading: boolean
     page: number
     pageSize: number
     searchParams: Record<string, any>
     selectedRows: T[]
     isEmpty: boolean
     
     load: (extraParams?) => Promise<void>
     search: (params?) => Promise<void>  // 重置到第一页后搜索
     reset: () => Promise<void>           // 重置搜索条件并刷新
     onPageChange: (page: number) => void
     onPageSizeChange: (size: number) => void
     setSearchParams: (params: Record<string, any>) => void
     clearSelection: () => void
   }
   
   内部使用 useCallback 和 useRef 避免不必要的重渲染

6. 将所有新组件/hook 加入对应 index.ts 导出

请确保：
- AppTable 在 loading 时仍显示表格框架（骨架），而不是完全空白
- AppTable 行选择 checkbox 联动（全选/半选/单选）正确
- AppPagination 跳页超出范围时自动修正（不报错）
- useTable 的 load 函数防止并发请求（后发的取消前发的）
```

**验收标准**：

- AppTable 渲染 100 行数据性能正常
- AppTable 列显隐配置弹窗正常
- AppPagination 跳页、每页条数切换正常
- useTable 初始化自动请求，search() 重置到第一页

---

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
