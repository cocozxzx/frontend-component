步骤 7 — 安装 shadcn/ui 基础类组件
目标：安装所有基础展示、反馈、布局类 shadcn/ui 组件，建立统一导出

Claude Code Prompt：

请帮我批量安装 shadcn/ui 基础组件并整理导出，具体如下：

1. 依次运行以下安装命令：
   npx shadcn@latest add button
   npx shadcn@latest add input
   npx shadcn@latest add textarea
   npx shadcn@latest add label
   npx shadcn@latest add badge
   npx shadcn@latest add card
   npx shadcn@latest add avatar
   npx shadcn@latest add separator
   npx shadcn@latest add tooltip
   npx shadcn@latest add popover
   npx shadcn@latest add hover-card
   npx shadcn@latest add scroll-area
   npx shadcn@latest add accordion
   npx shadcn@latest add collapsible
   npx shadcn@latest add toggle
   npx shadcn@latest add toggle-group
   npx shadcn@latest add aspect-ratio
   npx shadcn@latest add resizable
   npx shadcn@latest add skeleton
   npx shadcn@latest add alert
   npx shadcn@latest add progress
   npx shadcn@latest add dialog
   npx shadcn@latest add alert-dialog
   npx shadcn@latest add sheet
   npx shadcn@latest add drawer
   npx shadcn@latest add sonner
   npx shadcn@latest add command
   npx shadcn@latest add context-menu
   npx shadcn@latest add menubar

2. 安装完成后，创建 src/components/ui/index.ts，统一导出所有已安装的 ui 组件：
   export * from './accordion'
   export * from './alert'
   export * from './alert-dialog'
   export * from './aspect-ratio'
   export * from './avatar'
   export * from './badge'
   export * from './button'
   export * from './card'
   export * from './collapsible'
   export * from './command'
   export * from './context-menu'
   export * from './dialog'
   export * from './drawer'
   export * from './hover-card'
   export * from './input'
   export * from './label'
   export * from './menubar'
   export * from './popover'
   export * from './progress'
   export * from './resizable'
   export * from './scroll-area'
   export * from './separator'
   export * from './sheet'
   export * from './skeleton'
   export * from './sonner'
   export * from './textarea'
   export * from './toggle'
   export * from './toggle-group'
   export * from './tooltip'

3. 检查每个组件文件，确认以下 Tailwind class 使用的是 CSS Token 变量而非硬编码颜色：
   - bg-primary / text-primary-foreground（主色按钮）
   - bg-destructive / text-destructive-foreground（危险色）
   - bg-muted / text-muted-foreground（次要内容）
   - border-border（边框）
   - ring-ring（聚焦环）
   如有硬编码颜色（如 bg-blue-500），替换为对应 Token class

4. 在 src/main.tsx 中添加 Sonner Toaster 组件：
   import { Toaster } from '@/components/ui/sonner'
   在 App 根组件末尾添加 <Toaster position="top-right" richColors />

5. 创建 src/components/ui/typography.tsx（shadcn 无此组件，需手动创建）：
   导出以下组件（均为 React.forwardRef 形式，支持 className prop）：
   - H1: className="scroll-m-20 text-4xl font-extrabold tracking-tight lg:text-5xl"
   - H2: className="scroll-m-20 text-3xl font-semibold tracking-tight first:mt-0"
   - H3: className="scroll-m-20 text-2xl font-semibold tracking-tight"
   - H4: className="scroll-m-20 text-xl font-semibold tracking-tight"
   - P: className="leading-7 [&:not(:first-child)]:mt-6"
   - Blockquote: className="mt-6 border-l-2 pl-6 italic"
   - InlineCode: className="relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold"
   - Lead: className="text-xl text-muted-foreground"
   - Large: className="text-lg font-semibold"
   - Small: className="text-sm font-medium leading-none"
   - Muted: className="text-sm text-muted-foreground"
   将 typography.tsx 也加入 ui/index.ts 导出

6. 创建 src/components/ui/divider.tsx（水平/垂直分割线，含文字）：
   Props:
   - orientation?: 'horizontal' | 'vertical'（默认 horizontal）
   - children?: ReactNode（中间文字，如"或"）
   - className?: string
   
   实现：使用 Separator 组件，若有 children 则在中间显示文字（flex 布局，两侧 Separator）

7. 创建 src/components/ui/tag.tsx（标签组件）：
   Props:
   - variant?: 'default' | 'success' | 'warning' | 'destructive' | 'info' | 'outline'
   - size?: 'sm' | 'md'
   - closable?: boolean
   - onClose?: () => void
   - children: ReactNode
   - className?: string
   
   使用 cva 定义 variants，颜色对应 CSS Token：
   - default: bg-primary/10 text-primary border border-primary/20
   - success: bg-success/10 text-success border border-success/20
   - warning: bg-warning/10 text-warning border border-warning/20
   - destructive: bg-destructive/10 text-destructive border border-destructive/20
   - info: bg-info/10 text-info border border-info/20
   - outline: bg-transparent border border-border text-foreground
   closable=true 时右侧显示 X 图标，点击触发 onClose

将 divider.tsx 和 tag.tsx 也加入 ui/index.ts 导出。

完成后确认：所有组件可从 @/components/ui 正常导入，TS 无报错。
验收标准：

29 个 shadcn/ui 组件全部安装成功
import { Button, Input, Card } from '@/components/ui' 可正常使用
Toaster 在页面右上角正常显示
Typography、Divider、Tag 组件 TS 类型正确


## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
