### 步骤 9 — AppButton & AppInput & AppSelect

**目标**：封装最高频使用的三个基础业务组件，统一 loading、防抖、远程搜索等能力

---

**Claude Code Prompt**：

```
请帮我封装 AppButton、AppInput、AppSelect 三个业务组件，具体如下：

1. 创建 src/components/base/AppButton.tsx：
   
   在 shadcn Button 基础上扩展，Props（继承所有 Button props）：
   - loading?: boolean（显示加载状态）
   - throttleTime?: number（节流时间 ms，默认 0 即不节流）
   - permission?: string | string[]（权限控制，无权限时隐藏或禁用）
   - hideOnNoPermission?: boolean（无权限时隐藏，默认 false 即禁用）
   
   实现细节：
   - loading=true 时：左侧显示 Loader2 spin 图标，disabled=true，原 children 正常显示
   - throttleTime > 0 时：使用 useRef 记录上次点击时间，节流处理 onClick
   - permission 检查：从 usePermissionStore 获取当前用户权限列表，判断是否包含所需权限
     无权限 + hideOnNoPermission=true → return null
     无权限 + hideOnNoPermission=false → disabled=true，tooltip 提示"无操作权限"
   - 完整保留 shadcn Button 的所有 variant 和 size

2. 创建 src/components/base/AppInput.tsx：
   
   在 shadcn Input 基础上扩展，Props（继承所有 Input props）：
   - prefix?: ReactNode（前置内容，如图标）
   - suffix?: ReactNode（后置内容，如图标）
   - allowClear?: boolean（显示清除按钮，默认 false）
   - showCount?: boolean（显示字数统计，需配合 maxLength）
   - loading?: boolean（后置加载图标）
   - onClear?: () => void（清除回调）
   
   实现细节：
   - 整体使用 relative div 包裹，prefix/suffix 用 absolute 定位
   - prefix 在左侧：left-3，文字颜色 text-muted-foreground
   - suffix 在右侧：right-3，文字颜色 text-muted-foreground
   - 有 prefix 时 input padding-left 增大（pl-9）
   - 有 suffix/allowClear/showCount 时 padding-right 增大
   - allowClear：value 非空时显示 X 圆形按钮（XCircle icon），点击清空并触发 onChange('')
   - showCount：右下角显示 "当前字数/maxLength"，文字 text-xs text-muted-foreground
   - loading：suffix 位置显示 Loader2 spin

3. 创建 src/components/base/AppSelect.tsx：
   
   Props：
   - options?: Array<{ label: string; value: string | number; disabled?: boolean }>
   - value?: string | number
   - onChange?: (value: string | number) => void
   - placeholder?: string（默认"请选择"）
   - disabled?: boolean
   - loading?: boolean（显示加载状态）
   - allowSearch?: boolean（启用搜索过滤，默认 false）
   - remote?: boolean（远程搜索模式）
   - onSearch?: (keyword: string) => Promise<Array<{label: string; value: string | number}>>（远程搜索函数）
   - debounceTime?: number（远程搜索防抖，默认 300ms）
   - virtual?: boolean（虚拟滚动，选项超过 100 条时自动启用）
   - emptyText?: string（无数据提示，默认"暂无数据"）
   - className?: string
   - style?: React.CSSProperties
   
   实现细节：
   - 基于 shadcn Combobox 实现（Command + Popover）
   - allowSearch=true：顶部显示搜索输入框，本地过滤 options
   - remote=true：输入触发 onSearch，防抖 debounceTime ms，结果替换 options
   - remote loading 时下拉显示 Spin
   - virtual=true 或 options.length > 100：使用 @tanstack/react-virtual 渲染选项列表
   - 下拉高度最大 300px，超出滚动
   - 选中后显示 label，value 变化时触发 onChange

4. 创建 src/components/base/index.ts，导出所有 base 组件：
   export { AppButton } from './AppButton'
   export { AppInput } from './AppInput'
   export { AppSelect } from './AppSelect'

请确保：
- 所有组件完整 TypeScript 类型，无 any
- Props 使用 interface 定义，有 JSDoc 注释说明用途
- 组件内部 ref 使用 React.forwardRef 转发
- 样式只用 Tailwind class + cn()，不写 style 对象（除非必须）
```

**验收标准**：

- AppButton loading 状态图标正常显示，期间无法重复点击
- AppInput 清除按钮点击后 value 清空
- AppSelect 远程搜索防抖正常，不发送重复请求
- 所有组件 TypeScript 无 any 报错

---


## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
