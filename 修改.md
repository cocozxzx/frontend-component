### 步骤 24 — 基础组件预览页（30 个）

**目标**：为 30 个基础组件分别创建完整的预览页面

---

**Claude Code Prompt**：

```
请帮我创建所有基础组件的预览页面，每个页面文件放在 src/pages/components/basic/ 目录下，具体要求：

每个页面的统一结构：
1. PageHeader（组件名 + 描述 + 标签）
2. 多个 DemoSection（基础用法、尺寸、变体、状态、API 说明等）
3. 每个 DemoSection 内包含 ComponentDemo（渲染组件 + 代码）
4. 页面底部 PropsTable（Props 说明表格）

请按照以下规格逐一创建（每个文件约 150-300 行）：

1. ButtonPage.tsx：
   DemoSection：基础用法（variant: default/destructive/outline/secondary/ghost/link）
   DemoSection：尺寸（sm/md/lg/icon）
   DemoSection：状态（disabled、loading）
   DemoSection：图标按钮（左侧图标、右侧图标、纯图标）
   DemoSection：AppButton 扩展（loading 演示、throttle 演示）
   PropsTable：Button + AppButton 的所有 props

2. InputPage.tsx：
   DemoSection：基础用法
   DemoSection：禁用状态
   DemoSection：带前后缀（prefix: Search icon，suffix: Eye icon）
   DemoSection：AppInput 清除按钮
   DemoSection：AppInput 字数统计（maxLength=100）
   DemoSection：AppInput loading 状态
   PropsTable

3. SelectPage.tsx：
   DemoSection：基础用法（shadcn Select）
   DemoSection：AppSelect 本地搜索
   DemoSection：AppSelect 远程搜索（模拟 500ms 延迟）
   DemoSection：禁用状态
   DemoSection：VirtualSelect（生成 10000 条模拟数据）
   PropsTable

4. FormPage.tsx：
   DemoSection：基础表单（AppForm + FormField + Zod 校验）
   DemoSection：水平布局表单
   DemoSection：各类型字段综合示例
   DemoSection：ProForm Schema 驱动示例（展示 JSON Schema + 渲染结果）
   PropsTable（AppForm + ProForm）

5. TablePage.tsx：
   DemoSection：基础表格（shadcn Table，静态数据）
   DemoSection：AppTable（loading/空状态/斑马纹）
   DemoSection：排序和筛选
   DemoSection：行选择（checkbox）
   DemoSection：列显隐配置
   DemoSection：ProTable Schema 驱动示例（完整示例含搜索/分页/操作列）
   PropsTable

6. ModalPage.tsx：
   DemoSection：基础弹窗（shadcn Dialog）
   DemoSection：AppModal（async onConfirm）
   DemoSection：命令式 confirm（useModal hook 演示）
   DemoSection：命令式 alert（各 status 类型）
   DemoSection：自定义宽度
   PropsTable

7. DrawerPage.tsx：
   DemoSection：基础抽屉（四个方向）
   DemoSection：四种尺寸（sm/md/lg/full）
   DemoSection：带 footer 的抽屉
   DemoSection：loading 状态
   PropsTable

8. TabsPage.tsx：
   DemoSection：基础用法（shadcn Tabs，三个 tab）
   DemoSection：卡片样式
   DemoSection：禁用某个 tab
   DemoSection：带图标的 tab
   PropsTable

9. CardPage.tsx：
   DemoSection：基础卡片
   DemoSection：带 header/footer 的卡片
   DemoSection：StatCard 数值卡片（4 个并排）
   DemoSection：可点击卡片（hover 效果）
   PropsTable

10. BadgePage.tsx：
    DemoSection：shadcn Badge 所有 variant
    DemoSection：AppBadge 数字徽标
    DemoSection：AppBadge 红点模式
    DemoSection：AppBadge 状态模式（processing 波纹动画）
    DemoSection：AppBadge 包裹按钮/图标
    PropsTable

11. AvatarPage.tsx：
    DemoSection：图片头像
    DemoSection：文字头像（fallback）
    DemoSection：尺寸（sm/md/lg）
    DemoSection：头像组（堆叠）
    PropsTable

12. TooltipPage.tsx：
    DemoSection：基础用法（四个方向）
    DemoSection：延迟显示
    DemoSection：自定义内容
    PropsTable

13. PopoverPage.tsx：
    DemoSection：基础用法
    DemoSection：HoverCard
    DemoSection：自定义触发方式
    PropsTable

14. AlertPage.tsx：
    DemoSection：四种类型（default/destructive + 自定义 success/warning/info）
    DemoSection：带 description
    DemoSection：带关闭按钮
    DemoSection：带图标
    PropsTable

15. ToastPage.tsx：
    DemoSection：useToast 各方法演示（按钮触发）：success/error/warning/info/loading/promise
    DemoSection：带 description 和 action
    DemoSection：Sonner 原生 API
    PropsTable（useToast hook 方法说明）

16. ProgressPage.tsx：
    DemoSection：线形进度条（各 value）
    DemoSection：带文字标注
    DemoSection：自定义颜色
    DemoSection：圆形进度条（CSS 实现）
    PropsTable

17. SkeletonPage.tsx：
    DemoSection：文字骨架
    DemoSection：卡片骨架（模拟文章列表）
    DemoSection：表格骨架
    PropsTable

18. EmptyPage.tsx：
    DemoSection：基础空状态
    DemoSection：自定义图标和描述
    DemoSection：带操作按钮
    DemoSection：各场景示例（无搜索结果/无数据/无权限）
    PropsTable

19. ResultPage.tsx：
    DemoSection：success 结果
    DemoSection：error 结果
    DemoSection：warning 结果
    DemoSection：403/404/500 结果
    PropsTable

20. SpinPage.tsx：
    DemoSection：基础 Spin（各 size）
    DemoSection：带文字
    DemoSection：包裹内容（spinning true/false 切换）
    DemoSection：全屏 loading（按钮触发）
    PropsTable

21. PaginationPage.tsx：
    DemoSection：shadcn Pagination 基础
    DemoSection：AppPagination 完整功能
    DemoSection：简单模式（只有上下页）
    PropsTable

22. BreadcrumbPage.tsx：
    DemoSection：基础面包屑
    DemoSection：带图标
    DemoSection：自定义分隔符
    DemoSection：AppBreadcrumb（自动根据路由生成）
    PropsTable

23. StepsPage.tsx：
    DemoSection：水平步骤条（可点击切换当前步骤）
    DemoSection：垂直步骤条
    DemoSection：各状态（wait/process/finish/error）
    DemoSection：小尺寸
    PropsTable

24. CollapsePage.tsx：
    DemoSection：shadcn Accordion 基础
    DemoSection：允许多个同时展开
    DemoSection：带边框
    DemoSection：手风琴模式
    PropsTable

25. DividerPage.tsx：
    DemoSection：水平分割线
    DemoSection：带文字的分割线（居左/居中/居右）
    DemoSection：垂直分割线
    DemoSection：虚线
    PropsTable

26. TagPage.tsx：
    DemoSection：所有 variant（default/success/warning/destructive/info/outline）
    DemoSection：可关闭的 Tag（closable）
    DemoSection：不同大小（sm/md）
    DemoSection：Tag 组（动态添加删除）
    PropsTable

27. TypographyPage.tsx：
    DemoSection：标题（H1-H4）
    DemoSection：段落和行内元素（P/Blockquote/InlineCode/Lead/Large/Small/Muted）
    DemoSection：字体规范展示（所有字号、字重）
    DemoSection：文字颜色 Token 展示
    PropsTable

28. IconPage.tsx：
    所有 lucide-react 图标网格展示（带搜索框过滤）
    点击图标复制组件代码（如 `<Search size={16} />`）
    按类别分组显示
    注：动态 import 所有 lucide-react 图标

29. ScrollAreaPage.tsx：
    DemoSection：垂直滚动（固定高度内）
    DemoSection：水平滚动
    DemoSection：大列表（1000 项）
    PropsTable

30. WatermarkPage.tsx：
    DemoSection：文字水印
    DemoSection：多行文字水印
    DemoSection：自定义角度和透明度
    DemoSection：全屏水印（按钮触发）
    DemoSection：防篡改演示（说明 MutationObserver）
    PropsTable

注意：
- 所有 DemoSection 中的模拟数据（如表格数据）定义在文件顶部的常量中
- 代码字符串（ComponentDemo 的 code prop）使用模板字符串，展示真实可用的代码片段
- 每个页面有独立的状态管理（useState 在页面组件内）
- 30 个页面文件分批创建，每批 5-6 个，避免单次输出过大
```

**验收标准**：

- 30 个基础组件页面均可访问
- 每个组件展示所有主要 variant 和状态
- 代码高亮正确显示（无乱码）
- 复制代码按钮功能正常

---




## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm

