### 步骤 18 — 图表组件（ECharts 全套封装）

**目标**：封装 9 种图表 + StatCard，传简单 props 即可渲染，主题色自动同步

---

**Claude Code Prompt**：

```
请帮我封装完整的 ECharts 图表组件套件，具体如下：

1. 安装依赖：npm install echarts echarts-for-react

2. 创建 src/components/charts/BaseChart.tsx（基础图表封装）：

   Props：
   - option: EChartsOption（ECharts 配置项）
   - loading?: boolean（显示加载动画）
   - height?: string | number（默认 '300px'）
   - width?: string | number（默认 '100%'）
   - theme?: 'light' | 'dark'（默认跟随 useAppStore.colorMode）
   - onChartReady?: (chart: EChartsType) => void
   - onEvents?: Record<string, (params: any) => void>（ECharts 事件绑定）
   - renderer?: 'canvas' | 'svg'（默认 'canvas'）
   - className?: string

   实现细节：
   - 使用 echarts-for-react 的 ReactECharts 组件
   - 使用 ResizeObserver 监听容器尺寸变化，自动 resize
   - 监听 useAppStore.colorMode，dark 时使用 ECharts dark 主题
   - 监听 useAppStore.primaryColor，自动更新 option 中的 color 数组第一项
   - loading=true 时显示 ECharts 内置 loading 动画（showLoading）
   - 组件卸载时 dispose 实例（防内存泄漏）
   - 提供 getChartInstance() 方法（通过 ref）供外部调用 ECharts API

3. 创建通用图表颜色配置 src/components/charts/chartTheme.ts：

   - 导出 getChartColors(primaryColor: string): string[]
     基于主色用 chroma-js 生成 8 色系列：主色 + 派生色系
   
   - 导出 getChartTheme(colorMode: 'light' | 'dark', primaryColor: string)：
     返回完整 ECharts theme 配置对象（背景透明、网格线颜色、轴线颜色、tooltip 样式等）
     light 模式：网格线 #f0f0f0，轴线 #d9d9d9，文字 #666
     dark 模式：网格线 rgba(255,255,255,0.1)，轴线 rgba(255,255,255,0.2)，文字 #aaa

4. 创建 src/components/charts/LineChart.tsx（折线图）：

   Props：
   - data: Array<Record<string, any>>（数据数组）
   - xField: string（X 轴数据字段名）
   - yField: string | string[]（Y 轴数据字段名，支持多系列）
   - seriesNames?: string[]（多系列时的系列名称）
   - smooth?: boolean（平滑曲线，默认 false）
   - area?: boolean（面积图模式，默认 false）
   - areaOpacity?: number（面积透明度，默认 0.3）
   - showSymbol?: boolean（显示数据点，默认 true）
   - showLabel?: boolean（显示数据标签，默认 false）
   - connectNulls?: boolean（空数据连线，默认 false）
   - title?: string（图表标题）
   - legend?: boolean（显示图例，默认 true，多系列时）
   - tooltip?: boolean（显示 tooltip，默认 true）
   - grid?: object（ECharts grid 配置，控制边距）
   - yAxisFormatter?: (value: number) => string（Y 轴刻度格式化）
   - tooltipFormatter?: (params: any) => string（tooltip 格式化）
   - height?: string | number
   - loading?: boolean
   - className?: string

   实现：将 Props 转换为 ECharts option，传给 BaseChart

5. 创建 src/components/charts/BarChart.tsx（柱状图）：

   Props（在 LineChart 基础上新增）：
   - horizontal?: boolean（横向柱状图，默认 false）
   - stack?: boolean | string（堆叠，string 为堆叠组名，默认 false）
   - barWidth?: number | string（柱宽）
   - barMaxWidth?: number（柱最大宽度，默认 60）
   - showBackground?: boolean（显示柱子背景，默认 false）
   - radius?: number（柱子圆角，默认 4）
   （其余 Props 同 LineChart）

6. 创建 src/components/charts/PieChart.tsx（饼图/环形图）：

   Props：
   - data: Array<{ name: string; value: number; [key: string]: any }>
   - donut?: boolean（环形图，默认 false）
   - innerRadius?: string（环形内半径，donut=true 时有效，默认 '55%'）
   - outerRadius?: string（外半径，默认 '70%'）
   - roseType?: boolean（南丁格尔玫瑰图，默认 false）
   - showLabel?: boolean（显示标签，默认 true）
   - labelType?: 'inner' | 'outer' | 'center'（标签位置，默认 'outer'）
   - centerText?: ReactNode（环形中心文字，donut=true 时有效）
   - title?: string
   - legend?: boolean（默认 true）
   - legendPosition?: 'top' | 'bottom' | 'left' | 'right'（默认 'bottom'）
   - height?: string | number
   - loading?: boolean

   centerText 实现：ECharts graphic 组件绘制中心文字

7. 创建 src/components/charts/AreaChart.tsx：
   基于 LineChart，area 默认 true，areaOpacity 默认 0.4，smooth 默认 true

8. 创建 src/components/charts/RadarChart.tsx（雷达图）：

   Props：
   - data: Array<{ name: string; value: number[] }>（系列数据）
   - indicators: Array<{ name: string; max: number }>（雷达维度）
   - shape?: 'polygon' | 'circle'（默认 'polygon'）
   - filled?: boolean（是否填充，默认 true）
   - title?: string
   - legend?: boolean
   - height?: string | number
   - loading?: boolean

9. 创建 src/components/charts/ScatterChart.tsx（散点图）：

   Props：
   - data: Array<[number, number] | [number, number, number]>（[x, y] 或 [x, y, size]）
   - xName?: string（X 轴名称）
   - yName?: string（Y 轴名称）
   - symbolSize?: number | ((value: number[]) => number)（点大小，默认 10）
   - title?: string
   - height?: string | number
   - loading?: boolean

10. 创建 src/components/charts/GaugeChart.tsx（仪表盘）：

    Props：
    - value: number（当前值，0-100 的百分比）
    - min?: number（最小值，默认 0）
    - max?: number（最大值，默认 100）
    - title?: string（仪表盘标题）
    - unit?: string（单位，显示在数值后）
    - thresholds?: Array<{ value: number; color: string }>（颜色阈值，默认绿-黄-红三段）
    - splitNumber?: number（刻度分割数，默认 10）
    - height?: string | number
    - loading?: boolean

11. 创建 src/components/charts/HeatmapChart.tsx（日历热力图，GitHub 风格）：

    Props：
    - data: Array<[string, number]>（[日期字符串 'yyyy-MM-dd', 数值]）
    - year?: number（显示年份，默认当前年）
    - colorRange?: [string, string]（颜色范围，默认 ['#ebedf0', primaryColor]）
    - cellSize?: number（每格大小 px，默认 14）
    - cellGap?: number（格间距，默认 2）
    - tooltip?: boolean（悬浮显示日期和数值，默认 true）
    - months?: string[]（月份标签，默认中文）
    - weekdays?: string[]（周标签，默认中文）
    - height?: string | number
    - loading?: boolean

12. 创建 src/components/charts/StatCard.tsx（数值卡片 + 迷你图）：

    Props：
    - title: string（卡片标题）
    - value: number | string（主数值）
    - prefix?: ReactNode
    - suffix?: ReactNode
    - precision?: number
    - trend?: 'up' | 'down'（趋势方向）
    - trendValue?: string（趋势数值文字，如 '+12.5%'）
    - trendDesc?: string（趋势描述，如"较上月"）
    - miniChart?: {
        type: 'line' | 'bar'
        data: number[]（只需数值数组，X 轴自动）
      }（右侧迷你图）
    - icon?: ReactNode（左上角图标）
    - iconBg?: string（图标背景色，默认主色浅色背景）
    - loading?: boolean
    - onClick?: () => void
    - className?: string

    实现：Card 组件包裹，右侧迷你图使用 ECharts（隐藏坐标轴、网格、tooltip）

13. 创建 src/components/charts/index.ts 导出所有图表：
    export { BaseChart } from './BaseChart'
    export { LineChart } from './LineChart'
    export { BarChart } from './BarChart'
    export { PieChart } from './PieChart'
    export { AreaChart } from './AreaChart'
    export { RadarChart } from './RadarChart'
    export { ScatterChart } from './ScatterChart'
    export { GaugeChart } from './GaugeChart'
    export { HeatmapChart } from './HeatmapChart'
    export { StatCard } from './StatCard'

请确保：
- 所有图表切换 colorMode 时立即更新（不需要刷新）
- 所有图表切换 primaryColor 时立即更新
- BaseChart resize 防抖（50ms），避免频繁重绘
- StatCard miniChart 右侧图表高度自适应卡片高度
- GaugeChart value 变化时有动画过渡
```

**验收标准**：

- 所有图表在亮/暗色模式下均可读（颜色对比度足够）
- 切换主题色后 10 个图表颜色全部同步更新
- LineChart 多系列（5 条线）数据量 1000 点渲染无卡顿
- HeatmapChart 365 天数据正确渲染

---



## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
