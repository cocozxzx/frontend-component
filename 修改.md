### 步骤 12 — AppDatePicker & AppDateRangePicker & AppUpload

**目标**：封装日期选择、日期范围、文件上传三个复杂数据录入组件

---

**Claude Code Prompt**：

```
请帮我封装 AppDatePicker、AppDateRangePicker、AppUpload 三个组件，具体如下：

1. 创建 src/components/base/AppDatePicker.tsx：
   
   Props：
   - value?: Date | string | null
   - onChange?: (date: Date | null, dateStr: string) => void
   - placeholder?: string（默认"请选择日期"）
   - format?: string（显示格式，默认 'yyyy-MM-dd'）
   - disabled?: boolean
   - disabledDate?: (date: Date) => boolean（禁用特定日期）
   - showTime?: boolean（同时选择时间）
   - shortcuts?: Array<{ label: string; date: Date | (() => Date) }>（快捷选项）
   - allowClear?: boolean（默认 true）
   - className?: string
   
   实现细节：
   - 基于 shadcn Calendar + Popover
   - trigger 使用 AppInput（带日历图标 suffix，allowClear）
   - shortcuts 显示在日历左侧或下方，点击快速选择
   - 默认快捷选项：今天、昨天、本周、上周、本月、上月
   - showTime=true 时底部显示时分秒输入（三个 number input）
   - 日历使用 zhCN locale（来自 src/lib/date.ts）

2. 创建 src/components/base/AppDateRangePicker.tsx：
   
   Props：
   - value?: [Date | null, Date | null]
   - onChange?: (dates: [Date | null, Date | null], dateStrs: [string, string]) => void
   - placeholder?: [string, string]（默认 ['开始日期', '结束日期']）
   - format?: string
   - disabled?: boolean
   - disabledDate?: (date: Date) => boolean
   - shortcuts?: Array<{ label: string; range: [Date, Date] | (() => [Date, Date]) }>
   - allowClear?: boolean
   - className?: string
   
   实现细节：
   - Trigger 显示：[开始日期] 至 [结束日期]，带日历图标和清除按钮
   - Popover 内显示两个并排的月历
   - 选择时高亮范围（hover 预览效果）
   - 默认快捷选项：最近 7 天、最近 30 天、最近 90 天、本月、上月、今年

3. 创建 src/components/base/AppUpload.tsx：
   
   Props：
   - value?: UploadFile[]
   - onChange?: (files: UploadFile[]) => void
   - action?: string（上传 URL）
   - uploadFn?: (file: File, onProgress: (percent: number) => void) => Promise<string>（自定义上传，返回 URL）
   - accept?: string（文件类型限制，如 'image/*,.pdf'）
   - maxCount?: number（最大文件数量）
   - maxSize?: number（单文件大小限制，MB 为单位，默认 10）
   - multiple?: boolean（多文件选择，默认 false）
   - drag?: boolean（显示拖拽上传区域，默认 false）
   - listType?: 'text' | 'picture' | 'picture-card'（文件列表展示方式，默认 'text'）
   - disabled?: boolean
   - beforeUpload?: (file: File) => boolean | Promise<boolean>（上传前钩子，返回 false 取消上传）
   - onRemove?: (file: UploadFile) => boolean | Promise<boolean>（删除前钩子）
   - className?: string
   
   UploadFile 类型：
   interface UploadFile {
     uid: string
     name: string
     size?: number
     type?: string
     status: 'uploading' | 'done' | 'error' | 'removed'
     percent?: number（上传进度 0-100）
     url?: string（上传成功后的文件 URL）
     thumbUrl?: string（图片缩略图 URL）
     error?: string（错误信息）
     originFile?: File
   }
   
   实现细节（listType='text'）：
   - 上方：虚线框按钮区（Upload icon + "点击上传"文字），drag=true 时扩大为拖拽区
   - 下方：文件列表，每行：文件图标 + 文件名 + 文件大小 + 状态（上传中显示 Progress 条）+ 删除按钮
   - 拖拽：onDragOver 高亮边框，onDrop 触发上传
   
   实现细节（listType='picture'）：
   - 文件列表每行显示缩略图（图片文件）或文件类型图标
   
   实现细节（listType='picture-card'）：
   - 网格卡片展示，每张图片一个正方形卡片
   - hover 时显示删除和预览按钮
   - 上传进度圆形显示
   - 点击预览触发 Image 预览组件
   
   上传逻辑：
   - 优先使用 uploadFn，其次使用 action（axios POST，FormData）
   - 上传中通过 onProgress 回调更新 percent
   - maxSize 超出时显示 toast 错误提示，阻止上传
   - maxCount 超出时显示 toast 错误提示

4. 将所有新组件加入 src/components/base/index.ts 导出

请确保：
- AppDatePicker 选择后 trigger 正确显示格式化后的日期
- AppDateRangePicker 范围高亮效果（鼠标 hover 时预览范围）
- AppUpload 拖拽上传在 Chrome/Firefox/Safari 均正常
- 上传进度条实时更新（每次 onProgress 回调都触发 re-render）
```

**验收标准**：

- AppDatePicker 快捷选项点击正确设置日期
- AppDateRangePicker 两个日历联动，不允许结束日期早于开始日期
- AppUpload picture-card 模式图片预览正常
- 上传超出大小限制时有 Toast 提示，阻止上传

---


## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
