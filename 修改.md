### 步骤 16 — 数据展示组件（Timeline / Statistic / Descriptions / Anchor）

**目标**：封装时间线、统计数值、描述列表、锚点四个常用展示组件

---

**Claude Code Prompt**：

```
请帮我封装 Timeline、Statistic、Descriptions、Anchor 四个展示组件，具体如下：

1. 创建 src/components/display/Timeline.tsx 和 TimelineItem.tsx：

   TimelineItem Props：
   - label?: ReactNode（时间标签，显示在左侧或上方）
   - dot?: ReactNode（自定义节点图标）
   - color?: 'primary' | 'success' | 'warning' | 'error' | string（节点颜色，默认 'primary'）
   - position?: 'left' | 'right'（内容位置，alternate 模式下生效）
   - children?: ReactNode（内容）
   - className?: string

   Timeline Props：
   - mode?: 'left' | 'alternate' | 'right'（默认 'left'）
   - pending?: ReactNode | boolean（末尾显示"进行中"节点，true 时显示 loading 动画）
   - pendingDot?: ReactNode（自定义进行中节点图标）
   - reverse?: boolean（倒序显示，默认 false）
   - children: ReactNode（TimelineItem 集合）
   - className?: string

   实现细节：
   - 连接线：纵向 border-left（2px，颜色 border-border）
   - 节点圆点：8px 圆形，颜色对应 CSS Token（primary/success/warning/destructive）
   - mode='alternate'：奇数项内容在右，偶数项在左，标签在另一侧
   - pending=true：末尾节点显示 Loader2 spin，连接线延伸到底部
   - 动画：每个 TimelineItem 使用 animation-fade-in（stagger delay）

2. 创建 src/components/display/Statistic.tsx：

   Props：
   - title: ReactNode（标题）
   - value: number | string（主数值）
   - prefix?: ReactNode（数值前缀，如货币符号）
   - suffix?: ReactNode（数值后缀，如单位）
   - precision?: number（小数精度，默认 0）
   - formatter?: (value: number | string) => ReactNode（自定义数值渲染）
   - valueStyle?: React.CSSProperties（数值样式）
   - loading?: boolean（加载状态，显示 Skeleton）
   - countUp?: boolean（数字滚动动画，默认 true）
   - countUpDuration?: number（滚动时长 s，默认 2）
   - trend?: 'up' | 'down'（趋势箭头，ArrowUp/ArrowDown 图标）
   - trendValue?: string（趋势数值，如 '+12.5%'）
   - className?: string

   实现细节：
   - countUp=true 时使用 countup.js 实现数字从 0 滚动到 value
   - useEffect 监听 value 变化，重新触发动画
   - trend 箭头：up 时 text-success，down 时 text-destructive
   - loading 时：title 和 value 都替换为 Skeleton

3. 创建 src/components/display/Descriptions.tsx 和 DescriptionsItem.tsx：

   DescriptionsItem Props：
   - label: ReactNode（标签）
   - span?: number（占据列数，默认 1）
   - labelStyle?: React.CSSProperties
   - contentStyle?: React.CSSProperties
   - children: ReactNode（内容）

   Descriptions Props：
   - title?: ReactNode（标题）
   - extra?: ReactNode（右上角操作区）
   - bordered?: boolean（显示边框，默认 false）
   - column?: number | { xs: number; sm: number; md: number; lg: number }（每行列数，默认 3）
   - size?: 'sm' | 'md' | 'lg'（间距大小，默认 'md'）
   - layout?: 'horizontal' | 'vertical'（标签和内容的排列方向，默认 'horizontal'）
   - colon?: boolean（标签后显示冒号，默认 true）
   - labelStyle?: React.CSSProperties（全局标签样式）
   - contentStyle?: React.CSSProperties（全局内容样式）
   - children: ReactNode（DescriptionsItem 集合）
   - className?: string

   实现细节：
   - 使用 table 或 grid 布局
   - bordered 模式：所有单元格有边框，label 背景 bg-muted/50
   - 非 bordered：label 颜色 text-muted-foreground，内容 text-foreground
   - column 响应式：根据 useWindowSize 选择对应列数
   - span：DescriptionsItem 跨多列（colspan）

4. 创建 src/components/display/Anchor.tsx 和 AnchorLink.tsx：

   AnchorLink Props：
   - href: string（目标锚点 id，如 '#section-1'）
   - title: ReactNode（链接文字）
   - children?: ReactNode（嵌套子锚点）

   Anchor Props：
   - items?: Array<{ href: string; title: ReactNode; children?: ... }>（锚点配置，与 children 二选一）
   - offsetTop?: number（距顶部偏移，用于固定 Header 场景，默认 0）
   - targetOffset?: number（激活判断的目标偏移，默认 offsetTop）
   - affix?: boolean（固定到右侧，默认 true）
   - showInkInFixed?: boolean（固定时显示竖线，默认 false）
   - onChange?: (currentHref: string) => void（当前锚点变化回调）
   - children?: ReactNode（AnchorLink 集合）
   - className?: string

   实现细节：
   - 监听 scroll 事件（throttle 100ms）
   - 根据各锚点元素的 getBoundingClientRect().top 判断当前激活锚点
   - 激活项：左侧 3px 蓝色竖线 + 文字 text-primary
   - 点击锚点：scrollIntoView({ behavior: 'smooth' })，同时更新 URL hash
   - affix=true：position sticky top-offsetTop，跟随滚动
   - 嵌套锚点：缩进 pl-4 显示

5. 创建 src/components/display/Steps.tsx（步骤条，扩展 shadcn Stepper）：

   StepItem Props：
   - title: ReactNode
   - description?: ReactNode
   - icon?: ReactNode（自定义图标）
   - status?: 'wait' | 'process' | 'finish' | 'error'（可手动指定状态）

   Steps Props：
   - current: number（当前步骤，从 0 开始）
   - items: StepItem[]
   - direction?: 'horizontal' | 'vertical'（默认 'horizontal'）
   - size?: 'sm' | 'md'（默认 'md'）
   - labelPlacement?: 'horizontal' | 'vertical'（horizontal 方向时，标签在下方还是右侧）
   - onChange?: (current: number) => void（可点击步骤切换）
   - className?: string

   实现细节：
   - finish 状态：圆圈内显示 Check 图标，bg-primary text-primary-foreground
   - process 状态：圆圈内显示步骤数字，bg-primary text-primary-foreground，有脉冲动画
   - error 状态：圆圈内显示 X 图标，bg-destructive text-destructive-foreground
   - wait 状态：圆圈内显示步骤数字，bg-muted text-muted-foreground
   - 连接线：finish 段 bg-primary，其余 bg-border
   - 连接线动画：CSS transition width 从 0 到 100%

6. 将所有组件加入 src/components/display/index.ts 导出

请确保：
- Statistic countUp 在 value 变化时重新触发动画（不是只在 mount 时）
- Descriptions column 响应式在 resize 时正确切换列数
- Anchor 在有固定 Header（offsetTop=56）时正确判断激活锚点
- Steps 点击已完成的步骤可以跳转（onChange 触发）
```

**验收标准**：

- Timeline alternate 模式左右内容对齐正确
- Statistic 数字滚动动画流畅，精度正确
- Descriptions 响应式列数在移动端自动变为 1 列
- Anchor 滚动时激活状态平滑切换，不抖动

---



## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
