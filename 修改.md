### 步骤 10 — AppModal & AppDrawer & AppToast

**目标**：封装弹窗、抽屉、全局消息提示，支持命令式调用

---

**Claude Code Prompt**：

```
请帮我封装 AppModal、AppDrawer、AppToast，并实现命令式调用，具体如下：

1. 创建 src/components/base/AppModal.tsx：
   
   Props（继承 Dialog 所有 props）：
   - title?: string
   - description?: string
   - children?: ReactNode（内容区）
   - footer?: ReactNode（自定义底部，null 则不显示底部）
   - confirmText?: string（确认按钮文字，默认"确认"）
   - cancelText?: string（取消按钮文字，默认"取消"）
   - confirmVariant?: ButtonVariant（默认"default"）
   - loading?: boolean（确认按钮 loading 状态）
   - onConfirm?: () => void | Promise<void>（确认回调，支持 async）
   - onCancel?: () => void
   - width?: string | number（弹窗宽度，默认 520px）
   - closable?: boolean（显示右上角关闭按钮，默认 true）
   - maskClosable?: boolean（点击遮罩关闭，默认 true）
   
   实现细节：
   - 基于 shadcn Dialog
   - onConfirm 为 async 时，点击确认按钮自动设 loading=true，完成后关闭
   - footer=null 时不渲染底部 DialogFooter
   - width 通过 style 设置 DialogContent 的 max-width

2. 创建 src/hooks/useModal.ts，实现命令式弹窗：
   
   暴露以下方法：
   - modal.confirm(options): Promise<boolean>
     options: { title, content, type?: 'default'|'warning'|'danger', confirmText?, cancelText? }
     点击确认 resolve(true)，点击取消/关闭 resolve(false)
     type='danger' 时确认按钮为 destructive variant，显示警告图标
     type='warning' 时显示 AlertTriangle 图标（text-warning）
   
   - modal.alert(options): Promise<void>
     options: { title, content, type?: 'success'|'error'|'warning'|'info' }
     只有确认按钮，点击后 resolve
   
   实现方式：
   使用 Zustand store（useModalStore）管理弹窗队列状态
   在 AppLayout 中渲染 <ModalContainer />，消费 useModalStore 的状态
   useModal() hook 返回操作方法，内部调用 useModalStore.push(modalConfig)

3. 创建 src/components/base/AppDrawer.tsx：
   
   Props：
   - title?: string
   - description?: string
   - children?: ReactNode
   - footer?: ReactNode
   - side?: 'left' | 'right' | 'top' | 'bottom'（默认 'right'）
   - size?: 'sm' | 'md' | 'lg' | 'full'（对应宽度 320/480/640/100%）
   - loading?: boolean（内容区显示 Spin 遮罩）
   - confirmText?: string
   - cancelText?: string
   - onConfirm?: () => void | Promise<void>
   - onCancel?: () => void
   - open: boolean
   - onOpenChange: (open: boolean) => void
   
   基于 shadcn Sheet 实现，size 控制 SheetContent 的 className

4. 创建 src/hooks/useToast.ts，封装全局消息提示：
   
   基于 sonner toast，暴露以下方法：
   const { success, error, warning, info, loading, dismiss } = useToast()
   
   - success(message, options?): 绿色成功图标 + message
   - error(message, options?): 红色错误图标 + message
   - warning(message, options?): 橙色警告图标 + message
   - info(message, options?): 蓝色信息图标 + message
   - loading(message, options?): spin 图标 + message，返回 toastId
   - dismiss(toastId?): 关闭指定或全部 toast
   - promise(promise, options): loading → success/error 自动切换
   
   options 支持：{ duration?, description?, action?: { label, onClick } }
   
   同时导出 toast 对象供非 hook 场景使用（如在 axios 拦截器中）

5. 创建 src/stores/useModalStore.ts：
   管理命令式弹窗的队列状态
   interface ModalItem { id, type, title, content, options, resolve }
   actions: push(item) / remove(id)

6. 创建 src/components/base/ModalContainer.tsx：
   消费 useModalStore，渲染当前队列中的弹窗
   在 AppLayout 末尾引入此组件

7. 将所有新组件加入 src/components/base/index.ts 导出

请确保：
- modal.confirm() 返回真正的 Promise，await 等待用户操作
- AppDrawer size 四种尺寸在移动端正确响应
- useToast 可在任意组件和非组件（axios 拦截器）中使用
```

**验收标准**：

- `await modal.confirm({ title: '确认删除？', type: 'danger' })` 返回 true/false
- AppDrawer 四种 size 样式正确
- `useToast().success('操作成功')` 右上角弹出 Toast
- `useToast().promise(fetch(...), { loading: '提交中', success: '成功' })` 状态切换正常

---

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
