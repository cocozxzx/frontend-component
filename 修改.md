### 步骤 3 — 设计系统 & 主题 Token 体系

**目标**：建立完整主题切换系统，支持 5 种主题色 + 亮/暗/跟随系统模式

**执行内容**：

1. 创建 `src/styles/themes/` 多主题文件
2. 创建 `src/lib/theme-utils.ts`（主色 → 色阶算法）
3. 创建 `src/stores/useAppStore.ts`（主题配置 Zustand store）
4. 创建 `src/hooks/useTheme.ts`（主题切换逻辑）

---

**Claude Code Prompt**：

```
请帮我建立完整的主题系统，具体如下：

1. 创建 src/lib/theme-utils.ts，内容：
   使用 chroma-js 实现以下函数：
   
   - generateColorScale(hex: string): 根据主色 HEX 生成完整色阶对象
     返回 { primary, primaryLight, primaryDark, primaryHover, primaryActive }
     其中 primaryLight 是主色 opacity 0.1 的背景色
     primaryHover 比主色亮 10%
     primaryActive 比主色暗 10%
   
   - applyThemeColor(hex: string): void
     调用 generateColorScale 后，将结果写入 document.documentElement 的 CSS 变量
     覆盖 --primary 和相关衍生变量
   
   - hexToHsl(hex: string): string
     将 HEX 颜色转为 'H S% L%' 格式字符串（供 CSS 变量使用）
   
   - PRESET_COLORS 常量数组，包含 5 种预设色：
     { name: '科技蓝', value: '#1890FF' }
     { name: '活力橙', value: '#FA8C16' }
     { name: '翡翠绿', value: '#52C41A' }
     { name: '紫罗兰', value: '#722ED1' }
     { name: '玫瑰红', value: '#EB2F96' }

2. 创建 src/stores/useAppStore.ts，使用 Zustand + persist：
   
   interface AppConfig {
     // 布局
     layoutMode: 'top' | 'side' | 'mix'  // 默认 'top'
     sidebarCollapsed: boolean             // 默认 false
     sidebarWidth: number                  // 默认 200
     
     // 主题
     primaryColor: string                  // 默认 '#1890FF'
     colorMode: 'light' | 'dark' | 'system' // 默认 'light'
     
     // 界面
     showBreadcrumb: boolean               // 默认 true
     showFooter: boolean                   // 默认 true
     fixedHeader: boolean                  // 默认 true
     compactMode: boolean                  // 默认 false
     borderRadius: number                  // 默认 6（px）
     
     // 菜单
     accordionMenu: boolean                // 默认 true
   }
   
   Actions：
   - setLayoutMode(mode)
   - toggleSidebar()
   - setSidebarWidth(width)
   - setPrimaryColor(color): 调用 applyThemeColor，再保存到 store
   - setColorMode(mode): 切换 document.documentElement 的 dark class
   - setBorderRadius(radius): 实时更新 --radius CSS 变量
   - setCompactMode(compact): 切换 compact class 到 html 元素
   - resetConfig(): 重置为默认值
   - getConfigJson(): 返回当前配置的 JSON 字符串（供复制）
   
   persist 配置：name 为 'app-config'，存入 localStorage

3. 创建 src/hooks/useTheme.ts：
   - 导出 useTheme() hook
   - 在组件挂载时（useEffect）根据 store 中的 colorMode 初始化 dark class
   - 监听系统主题变化（matchMedia），当 colorMode 为 'system' 时自动跟随
   - 在 store 的 primaryColor 变化时调用 applyThemeColor
   - 在 store 的 borderRadius 变化时更新 --radius CSS 变量
   - 返回 { colorMode, setColorMode, primaryColor, setPrimaryColor, isDark }

4. 在 src/main.tsx 中：
   创建 AppInitializer 组件，调用 useTheme() 确保主题在应用启动时初始化
   将 AppInitializer 包裹在 App 根组件内

完成后在浏览器控制台验证：
- document.documentElement.style.getPropertyValue('--primary') 有值
- 调用 store 的 setPrimaryColor('#52C41A') 后颜色变化
```

**验收标准**：

- 切换主题色，页面颜色实时变化
- 刷新页面，主题配置保留（localStorage 持久化）
- 系统暗色模式时，colorMode='system' 自动跟随

---

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
