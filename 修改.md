### 步骤 6 — 主题配置抽屉（ThemeDrawer）

**目标**：右侧滑出的可视化配置面板，实时预览所有配置，刷新后保留

---

**Claude Code Prompt**：

```
请帮我实现主题配置抽屉组件 ThemeDrawer，具体如下：

1. 创建 src/components/layout/ThemeDrawer.tsx：
   使用 shadcn/ui Sheet 组件，side="right"，宽度 320px
   
   内部配置项分组如下：

   【导航布局】
   三个选项卡式按钮：
   - "顶部导航"（LayoutTemplate icon）
   - "侧边导航"（PanelLeft icon）
   - "混合导航"（LayoutDashboard icon）
   点击切换 useAppStore.layoutMode

   【整体风格】
   三个按钮：亮色 / 暗色 / 跟随系统
   图标：Sun / Moon / Monitor

   【主题色】
   5 个预设色圆形色块（来自 PRESET_COLORS）
   点击选中时显示 check 图标
   最后一个是自定义：<input type="color"> 原生颜色选择器
   选择后调用 useAppStore.setPrimaryColor(hex)

   【界面设置】
   以下每项为 label + Switch 组件：
   - 固定顶栏（fixedHeader）
   - 显示面包屑（showBreadcrumb）
   - 显示页脚（showFooter）
   - 紧凑模式（compactMode）
   - 手风琴菜单（accordionMenu）

   【尺寸调节】
   菜单宽度：Slider，min=160 max=280 step=10，仅 side/mix 模式下显示
   全局圆角：Slider，min=0 max=16 step=2，值变化时实时更新 --radius CSS 变量

   【底部操作区】
   - "重置配置"按钮（ghost variant，调用 resetConfig()）
   - "复制配置"按钮（outline variant，调用 getConfigJson() 并复制到剪贴板，显示 Toast "配置已复制"）

2. ThemeDrawer 通过 prop open + onOpenChange 控制显示
   在 AppHeader 的设置按钮点击时打开

3. 创建 src/components/layout/ThemeColorPicker.tsx（抽取颜色选择部分）：
   Props: value: string, onChange: (color: string) => void
   预设色 + 自定义输入

4. 所有配置变更立即生效（不需要确认），实时更新 CSS 变量和 store

请确保：
- 每个配置项有明确的 label 和当前值显示
- Switch 切换有 150ms 动画
- 颜色选择后整个页面颜色立即变化
- Slider 拖动时实时预览（不是松开才更新）
```

**验收标准**：

- 所有配置实时生效
- 刷新页面配置保留
- 复制配置功能正常（粘贴板有 JSON 内容）
- 暗色模式切换后 ThemeDrawer 自身样式也正确反转

## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm
