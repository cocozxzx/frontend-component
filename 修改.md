### 步骤 19 — 地图组件（高德 Maps 封装）

**目标**：封装高德地图 4 种模式，动态加载 SDK 不影响首屏

---

**Claude Code Prompt**：

```
请帮我封装高德地图组件套件，具体如下：

1. 创建 src/lib/amap.ts（高德地图 SDK 动态加载器）：

   - 导出 loadAMap(key: string, plugins?: string[]): Promise<typeof AMap>
     检查 window.AMap 是否已存在，存在直接 resolve
     否则动态创建 script 标签，src 为：
     https://webapi.amap.com/maps?v=2.0&key={key}&plugin={plugins.join(',')}
     加载完成 resolve(window.AMap)，失败 reject
   
   - 导出 AMAP_KEY 常量（从 import.meta.env.VITE_AMAP_KEY 读取）
   
   - 导出 DEFAULT_CENTER: [number, number] = [116.397428, 39.90923]（北京）
   - 导出 DEFAULT_ZOOM = 12

2. 创建 src/components/map/BaseMap.tsx（地图基础组件）：

   Props：
   - mapKey?: string（高德 key，默认使用 AMAP_KEY 环境变量）
   - center?: [number, number]（中心点 [lng, lat]，默认 DEFAULT_CENTER）
   - zoom?: number（缩放级别，默认 DEFAULT_ZOOM）
   - minZoom?: number
   - maxZoom?: number
   - mapStyle?: string（地图样式，如 'amap://styles/dark'）
   - showScale?: boolean（显示比例尺，默认 false）
   - showToolbar?: boolean（显示缩放工具栏，默认 false）
   - dragEnable?: boolean（启用拖拽，默认 true）
   - scrollWheel?: boolean（滚轮缩放，默认 true）
   - onMapLoad?: (map: AMap.Map) => void（地图加载完成回调）
   - onMapClick?: (e: AMap.MapsEvent) => void
   - height?: string | number（默认 '400px'）
   - width?: string | number（默认 '100%'）
   - loading?: boolean（外部 loading 状态）
   - children?: ReactNode（可传入其他地图子组件）
   - className?: string

   实现细节：
   - useEffect 中调用 loadAMap，加载完成后创建 AMap.Map 实例
   - 组件卸载时 map.destroy()
   - 加载中显示 Skeleton 占位
   - 通过 Context（MapContext）将 map 实例共享给子组件
   - colorMode 为 dark 时自动切换 mapStyle 为暗色（'amap://styles/dark'）

3. 创建 src/components/map/MarkerMap.tsx（标记点地图）：

   Marker 数据类型：
   interface MarkerData {
     id: string | number
     lng: number
     lat: number
     title?: string
     info?: ReactNode | string（信息窗口内容）
     icon?: string（自定义图标 URL）
     iconSize?: [number, number]（图标大小）
     color?: string（使用内置标记时的颜色）
   }

   Props（继承 BaseMap 所有 Props）：
   - markers: MarkerData[]
   - onMarkerClick?: (marker: MarkerData, e: AMap.MapsEvent) => void
   - showInfoWindow?: boolean（点击标记显示信息窗口，默认 true）
   - fitMarkers?: boolean（自动调整视野包含所有标记，默认 true）
   - clusterEnabled?: boolean（启用点聚合，默认 false，超过 50 个点建议开启）

   实现细节：
   - markers 变化时，先清除旧标记（map.remove），再添加新标记
   - 信息窗口：AMap.InfoWindow，内容为 ReactDOM.renderToStaticMarkup(info) 或字符串
   - fitMarkers：使用 map.setFitView(markers)
   - clusterEnabled：使用 AMap.MarkerClusterer 插件

4. 创建 src/components/map/HeatMap.tsx（热力图）：

   HeatPoint 类型：{ lng: number; lat: number; weight?: number }

   Props（继承 BaseMap 所有 Props）：
   - points: HeatPoint[]
   - radius?: number（热力点半径，默认 25）
   - opacity?: [number, number]（最小/最大透明度，默认 [0, 0.8]）
   - gradient?: Record<string, string>（颜色渐变，默认蓝→绿→黄→红）
   - blur?: number（模糊半径，默认 35）

   实现：使用 AMap.HeatMap 插件

5. 创建 src/components/map/PolylineMap.tsx（路线/区域地图）：

   PolylineData 类型：
   {
     id: string | number
     path: Array<[number, number]>（路径点数组 [[lng, lat], ...]）
     strokeColor?: string
     strokeWeight?: number（线宽，默认 4）
     strokeOpacity?: number（透明度，默认 0.8）
     strokeStyle?: 'solid' | 'dashed'（默认 'solid'）
   }

   PolygonData 类型：
   {
     id: string | number
     path: Array<[number, number]>（多边形顶点）
     strokeColor?: string
     fillColor?: string
     fillOpacity?: number（默认 0.3）
   }

   Props（继承 BaseMap 所有 Props）：
   - polylines?: PolylineData[]（路线数组）
   - polygons?: PolygonData[]（多边形数组）
   - onPolylineClick?: (data: PolylineData) => void
   - onPolygonClick?: (data: PolygonData) => void

6. 创建 src/components/map/index.ts 导出：
   export { BaseMap } from './BaseMap'
   export { MarkerMap } from './MarkerMap'
   export { HeatMap } from './HeatMap'
   export { PolylineMap } from './PolylineMap'
   export type { MarkerData, HeatPoint, PolylineData, PolygonData }

7. 在 .env.example 中添加：
   VITE_AMAP_KEY=your_amap_key_here

请确保：
- SDK 只加载一次（多个地图组件不重复加载）
- 组件卸载时彻底清理地图实例（无内存泄漏）
- markers 数组变化时正确差量更新（不闪烁）
- 暗色模式时地图自动切换为暗色样式
```

**验收标准**：

- BaseMap 首次加载显示 Skeleton，加载完成平滑过渡
- MarkerMap 100 个标记点正常展示，点击显示信息窗口
- HeatMap 热力图颜色渐变正确
- colorMode 切换为 dark 时地图样式同步更新

---



## 执行注意事项
1. **写代码前先阅读文件**,有什么问题提出来，完成确认后再进行编码
2. **记忆文件**，自己创建一个文件用于我让你做了那些东西，以后好溯源找记忆
3. **严格按步骤顺序执行**，步骤有依赖关系
4. **每个步骤完成后验收**，通过后再执行下一步
5. **遇到 TypeScript 报错**：优先修复类型错误，不要用 `any` 绕过
6. **组件 import 路径约定**：
   - shadcn/ui 原生：`@/components/ui`
   - 业务封装：`@/components/base`
   - 图表：`@/components/charts`
   - Pro 组件：`@/components/pro/ProTable` 或 `@/components/pro/ProForm

